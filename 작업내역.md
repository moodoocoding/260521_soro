# 작업 내역

이 문서는 `260521_soro` 저장소에서 진행하는 작업을 시간순으로 기록합니다.
코드를 수정할 때마다 이 파일에 항목을 추가합니다.

---

## 2026-08-14

### 저장소 클론
- `https://github.com/moodoocoding/260521_soro.git` 을 `main` 브랜치 기준으로 로컬에 클론.
- 작업용 브랜치 `modify` 생성 및 전환.

### 전체 코드 리뷰 수행 (커밋 없음, 조사만 진행)
`app.js`(~7,900줄), `index.html`, `style.css` 전체를 7가지 관점(정확성/비동기, 관리자 권한 경계, 파일 간 참조, 재사용성, 단순화, 효율성, 설계 깊이)으로 리뷰. 검증을 거쳐 아래 10건을 확정. **아직 아무것도 수정하지 않은 상태.**

| # | 심각도 | 요약 | 위치 | 상태 |
|---|--------|------|------|------|
| 1 | 심각(보안) | 백엔드(Apps Script) API에 서버 측 권한 검사가 전혀 없음 — API URL만 알면 누구나 학생 데이터 조회/삭제, 설정 변경 가능 | app.js:7392 등 (doPost, 7125-7887 임베디드 백엔드 코드) | 코드 수정 완료, **실제 배포 대기 중** (아래 상세 참고) |
| 2 | 심각(보안) | `checkIsAdmin()`이 localStorage 값 비교뿐이라 관리자 권한이 클라이언트에서 위조 가능 | app.js:641 | 코드 수정 완료 (아래 상세 참고) |
| 3 | 정정됨 | 비-젭퀴즈 공모전이 학생에게 항상 "마감"으로 표시됨 → **의도된 동작으로 확인됨** (자동 개폐 로직의 운영상 문제로 수동 통제 중) | app.js:649 | 의도된 동작 (수정 불필요) |
| 4 | 심각(보안) | `submitContest` 백엔드가 회차/잠금 상태/제출자 신원을 검증하지 않아 마감된 공모전 제출이나 가짜 신원 제출이 가능 (단, 실존 학생 사칭까지는 미해결 — 아래 참고) | app.js:7229 | 배포 및 검증 완료 |
| 5 | 중간 | `fetchContestLocks()`의 클램프 로직 때문에 활성 회차를 "1"로 설정해도 항상 "3"으로 되돌아감 | app.js:29 | 수정 완료 |
| 6 | 중간(추정) | 관리자가 젭퀴즈 회차를 빠르게 전환하면 비동기 요청 경쟁으로 오래된 회차 데이터가 화면에 덮어써질 수 있음 | app.js:6560 | 수정 완료 |
| 7 | 중간(추정) | 공모전 상세 드로어를 빠르게 전환하면 다른 공모전의 제출 상태가 잘못 표시될 수 있음 | app.js:2385 | 수정 완료 |
| 8 | 중간(추정) | AI 캘리그라피 생성이 느리게 끝나는 사이 다른 공모전으로 이동해 업로드하면 이전 이미지가 제출될 수 있음 | app.js:3416 | 수정 완료 |
| 9 | 중간 | 관리자 상세평가 버튼(`openAdminEvalModal`)이 정의되지 않은 함수를 호출함 — 클릭해도 아무 동작 안 함 | app.js:6152 | 수정 완료 |
| 10 | 낮음 | 관리자 "표 뷰" 토글이 실제로는 숨겨진 갤러리 컨테이너에 렌더링해서 항상 "준비 중" placeholder만 보임 | app.js:5859 | 수정 완료 |

### 1번 항목 수정: 백엔드에 서버 측 관리자 인증 추가
관련 항목: **#1** (백엔드 권한 검사 부재). 계획 문서: `.claude/plans/parallel-launching-wave.md` (세션 로컬 경로).

**변경 내용 (app.js):**
- 로그인 액션(`login`)에서 인증 성공 시, `requestData.userKey`가 하드코딩된 관리자 식별자(`5_1_27_김태호`)와 일치하면 `Utilities.getUuid()`로 세션 토큰을 발급해 `PropertiesService`에 12시간 만료로 저장하고 응답에 `adminToken`으로 포함.
- `isValidAdminToken(token)` 헬퍼 추가.
- 관리자 전용 액션 9개(`deleteUser`, `getAllSubmissions`(단, `contestId==="all"`인 전체 조회일 때만), `updateSubmissionPrizeStatus`, `updateActiveZepRound`, `updateContestLock`, `updateSubmissionStarStatus`, `getZepQuizStats`, `updateClassPrizeStatus`, `updateClassStudentCount`)에 토큰 검사 추가.
- `deleteSubmission`: 삭제 대상의 실제 소유자(`targetUsername`)와 `requestData.studentUsername`이 일치하거나 유효한 관리자 토큰이 있어야 삭제되도록 변경 (본인 취소는 그대로 허용, 타인이 아이디만 알고 삭제하던 기존 허점도 함께 막힘).
- `resetPassword`: 관리자 계정(`5_1_27_김태호`)에 한해서만 유효한 관리자 토큰을 요구 (조사 중 발견한 연관 취약점 — 안 막으면 비밀번호 재설정으로 토큰 발급 자체를 우회 가능했음).
- 클라이언트: `handleLogin`/`handleSignUp`/`handleResetPassword`가 응답의 `adminToken`을 `currentUser`와 localStorage에 저장. `getAdminToken()` 헬퍼 추가. 위 9개 액션 및 관리자용 `deleteSubmission` 호출부 전부에 `adminToken` 필드 추가. 학생 본인 취소용 `deleteSubmission` 호출부 3곳에는 `studentUsername` 필드 추가.

**계획에서 살짝 벗어난 부분:** `getAllSubmissions`는 원래 계획대로 전부 막으면 로그인 없이도 동작해야 하는 공개 갤러리(키링 공모전, 온라인 도서관)가 깨지는 걸 구현 중 발견해서, `contestId === "all"`(관리자 대시보드의 전체 일괄 조회)일 때만 토큰을 요구하도록 범위를 좁힘. 특정 공모전 지정 조회(공개 갤러리용)는 기존처럼 그대로 둠.

**검증 상태:** 코드 레벨 검토 및 `node --check`로 문법 확인 완료. 실제 동작 확인은 배포 후에만 가능.

**✅ 배포 및 검증 완료 (2026-08-14):** 사용자가 script.google.com에 새 버전으로 배포 완료. 배포 직후 curl로 테스트했을 때 일시적으로 "페이지를 찾을 수 없음" 오류가 떴으나(전파 지연으로 추정), 몇 분 후 재시도하니 정상화됨. 최종 검증 결과:
- `getAllSubmissions`(`contestId:"all"`, 토큰 없음) → 차단됨 ✅
- `getAllSubmissions`(`contestId:"keyring"`, 공개 갤러리) → 정상 작동, 로그인 불필요 유지 ✅
- `getZepQuizStats`(토큰 없음) → 차단됨 ✅
- `deleteUser`(토큰 없음) → 차단됨 ✅
- `getAllSubmissions`(가짜 토큰) → 차단됨 (토큰 값 실제 검증 확인) ✅

남은 것: 실제 관리자 계정으로 로그인해 관리자 대시보드 정상 동작 확인 (Claude가 비밀번호를 모르므로 사용자가 직접 확인 필요).

**🔙 롤백용 백업:** 수정 전 원본 Apps Script 코드를 [doc/backend_backup_2026-08-14_before_admin_auth.gs](doc/backend_backup_2026-08-14_before_admin_auth.gs)에 저장해 뒀습니다. 새 코드를 배포한 뒤 문제가 생기면, script.google.com 편집기 내용을 이 백업 파일 내용으로 다시 덮어쓰고 "배포 관리 → 편집 → 새 버전 → 배포"만 하면 즉시 이전 상태로 되돌릴 수 있습니다. (참고로 git 커밋 이력에도 원본이 그대로 남아있어 이 파일이 없어져도 `git show`로 복구 가능합니다.)

**⚠️ 아직 안 끝남 — 사람이 직접 해야 할 일:**
1. `app.js`의 7125~7887줄(관리자 인증 부분 포함, 새로 삽입된 내용 포함) 전체를 복사해서 **script.google.com의 실제 Apps Script 프로젝트에 붙여넣기**.
2. **배포 → 배포 관리 → 편집(연필) → 새 버전 → 배포** (반드시 기존 배포의 "새 버전"으로 — 그래야 `GOOGLE_SHEET_API_URL`이 안 바뀜).
3. 배포 후 확인: (1) 비관리자 로그인 시 관리자 액션이 거부되는지, (2) 관리자 로그인은 정상 동작하는지, (3) 키링/도서관 공개 갤러리가 여전히 로그인 없이 보이는지, (4) 토큰 없이 `curl`로 `getAllSubmissions`(`contestId:"all"`) 호출 시 거부되는지.

이번 범위에서 제외한 것 (별도 항목으로 남김): `submitContest`의 회차/잠금 검증 부재(#4), 일반 학생 대상 `resetPassword`의 소유권 미검증, 레이스 컨디션들(#6,#7,#8), `openAdminEvalModal` 미정의(#9), 관리자 "표 뷰" 렌더링 오류(#10).

### 2번 항목 수정: `checkIsAdmin()`이 로그인 토큰도 함께 확인하도록 변경
관련 항목: **#2** (관리자 판별 클라이언트 위조 가능).

**변경 내용 (app.js:641):** `checkIsAdmin()`이 기존 학년/반/번호/이름 비교에 더해 `currentUser.adminToken`이 존재하는지도 확인하도록 수정. 1번 수정으로 이 토큰은 실제 관리자 비밀번호로 로그인해야만 서버가 발급해주므로, devtools에서 프로필 값만 대충 조작해서는 더 이상 관리자 UI가 뜨지 않음.

**한계(솔직히 밝힘):** 이건 완전한 보안 장치는 아닙니다 — 클라이언트 코드는 원리적으로 사용자가 다 들여다보고 조작할 수 있어서, `adminToken` 필드에 아무 문자열이나 채워 넣으면 이 클라이언트 쪽 체크는 여전히 통과됩니다. 진짜 방어선은 1번 수정으로 이미 만들어진 **서버 측 토큰 검증**입니다 — 실제 관리자 액션(삭제/조회/설정변경)은 그 가짜 토큰으로는 서버가 다 거부합니다. 이번 수정은 UI가 잘못된 상태를 보여주지 않도록 하는 보정 성격이 큽니다.

**남은 연결 문제:** `getContestStatus()`(app.js:649)가 여전히 `checkIsAdmin()`이 참이면 모든 공모전을 "active"로 반환해서, 위 한계와 마찬가지로 정교하게 값을 조작하면 마감된 공모전의 제출 폼을 열 수는 있습니다. 다만 실제로 제출이 접수되려면 `submitContest`가 서버에서 검증을 해야 하는데 그게 아직 안 되어 있어서(**#4**), 이 부분의 완전한 해결은 #4 수정과 묶여 있습니다.

**검증 상태:** `node --check` 문법 확인 완료. 클라이언트 전용 변경이라 별도 배포(Apps Script) 불필요 — 프론트엔드가 배포되면 바로 적용됨.

**✅ 사용자 확인 (2026-08-14):** 라이브 사이트에서 관리자 로그인/대시보드 접근 문제 해결됨.

### 4번 항목 수정: `submitContest`에 회차/신원 검증 추가
관련 항목: **#4** (`submitContest` 검증 부재).

**논의된 범위 결정:** 키링/도서관 등 비-젭퀴즈 공모전도 서버에서 API 직접 제출을 막을지 사용자에게 확인 → **막기로 결정**. 나중에 이 공모전들을 열려면 코드를 고치거나 관리자 토큰으로 우회해야 함 (아직 잠금 해제용 관리자 UI는 없음).

**변경 내용 (app.js, `submitContest` 핸들러):**
1. **회차 검증**: `zepquiz_N` 제출은 Settings 시트의 `zepquiz_active_round`와 N이 일치할 때만 허용 (관리자 토큰이 있으면 예외).
2. **비-젭퀴즈 공모전 차단**: `zepquiz_`로 시작하지 않는 `contestId`는 관리자 토큰 없이는 전부 거부. **단, `pixelart_draft`(픽셀아트 에디터의 임시저장 내부 기능)는 실제 "공모전"이 아니라서 예외로 허용** — 처음에 이걸 놓쳐서 학생들의 임시저장 기능이 깨질 뻔했다가 구현 중 발견해서 고침.
3. **제출자 신원 검증**: `entry.studentUsername/studentGrade/studentClass/studentNumber/studentName`이 Users 시트의 실제 가입 정보와 정확히 일치해야만 접수. 하나라도 다르면 "제출자 정보가 가입된 학생 정보와 일치하지 않습니다" 오류.

**⚠️ 알려진 한계 (사용자와 논의 후 현재 범위로 확정):** 3번 신원 검증은 "존재하지 않는 가짜 신원으로 제출"만 막습니다. **진짜 존재하는 다른 학생을 사칭하는 건 막지 못합니다** — 이 시스템엔 사전에 정해진 학생 명부가 없고 `signUp`으로 누구나 자기가 원하는 학년/반/번호/이름을 등록하는 구조라서, "Users 시트에 그 조합이 있는지"만으로는 요청을 보낸 사람이 진짜 그 학생인지 증명할 수 없습니다. 실제로 다른(이미 가입된) 학생의 정보는 공개 갤러리에도 노출되어 있어서(예: 키링 갤러리 응답에 `studentName`/`studentGrade` 등이 그대로 포함됨), 그 값을 그대로 복사해서 보내면 비밀번호 없이도 이 검증을 통과합니다. 진짜 사칭까지 막으려면 관리자처럼 학생도 로그인 시 토큰을 발급받아 매 제출마다 검증하는 구조가 필요한데, 이건 학생용 로그인/제출/취소 등 전 경로에 손대는 별도의 큰 작업이라 **이번 범위에서는 제외하기로 결정함** (2026-08-14, 사용자 확인).

**검증 상태:** `node --check` 문법 확인 완료. 클라이언트 정상 제출 흐름(모든 실제 제출은 로그인된 `currentUser` 값을 그대로 사용)은 항상 Users 시트와 일치하므로 영향 없음.

**✅ 배포 및 검증 완료 (2026-08-14):** 새 버전 배포 후 가짜 학생 정보로 4가지 케이스 curl 테스트 — 안 열린 회차 차단, 비-젭퀴즈 공모전 차단, (열린 회차/`pixelart_draft`) + 가짜 신원은 각각 신원 검증 단계에서 차단, 전부 의도대로 작동 확인. 테스트는 전부 거부되도록 설계해서 실제 시트에 데이터가 쓰이지 않음.


### 5번 항목 수정: 젭퀴즈 1회차 클램프 버그
관련 항목: **#5**.

**변경 내용 (app.js:29):** `fetchContestLocks()`에서 서버가 알려준 활성 회차를 `parsedRound < 2 ? 3 : parsedRound`로 되돌리던 조건을 `parsedRound < 1 ? 3 : parsedRound`로 수정. 기존엔 관리자가 1회차를 활성으로 설정해도(`setAsActiveZepRound`는 정상 저장됨) 학생 화면이 새로고침되거나 `fetchContestLocks()`가 다시 호출될 때마다 3회차로 강제로 되돌아갔음.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요, `main`에 병합 후 프론트엔드만 배포되면 바로 적용됨.

**검증 상태:** `node --check` 문법 확인 완료.

### 6번 항목 수정: 젭퀴즈 회차 조회 레이스 컨디션
관련 항목: **#6**.

**변경 내용 (app.js, `fetchZepQuizDataAndRender`):** 관리자가 회차 드롭다운을 빠르게 여러 번 바꾸면(`handleZepRoundChange`가 매번 이 함수를 await 없이 호출) 오래된 요청의 응답이 최신 요청보다 늦게 도착해 화면을 이전 회차 데이터로 덮어쓸 수 있었음. 요청마다 증가하는 토큰(`zepQuizFetchRequestToken`)을 발급해서, 응답이 왔을 때 "이 요청이 여전히 가장 최근 요청인지" 확인하고 아니면 그냥 버리도록 수정.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. 레이스 컨디션이라 실제 브라우저에서 회차를 빠르게 연타 전환해보며 확인하는 게 가장 확실함 (제가 재현하기는 어려움).

### 7번 항목 수정: 공모전 상세 드로어 레이스 컨디션
관련 항목: **#7**. 6번과 같은 종류의 문제(오래된 비동기 응답이 최신 화면을 덮어씀)를 같은 패턴(요청 토큰)으로 해결.

**변경 내용 (app.js, `checkAndRenderSubmissionArea`):** 학생이 공모전 상세 드로어를 빠르게 열었다 닫고 다른 공모전을 열면, 먼저 연 공모전의 "제출 내역 조회" 응답이 나중에 도착해 지금 보고 있는 다른 공모전의 제출 상태(제출완료/미제출 폼)를 잘못 덮어쓸 수 있었음. `submissionAreaRequestToken`을 추가해서, 서버 응답을 받은 시점에 "내가 여전히 가장 최근 요청인지" 확인 후 아니면 화면 갱신을 건너뛰도록 수정.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. 레이스 컨디션이라 실제로는 드로어를 빠르게 여러 번 전환해보며 확인하는 게 가장 확실함.

### 8번 항목 수정: AI 캘리그라피 생성 레이스 컨디션
관련 항목: **#8**. 6·7번과 같은 종류의 문제(오래된 비동기 작업이 최신 상태를 덮어씀).

**변경 내용 (app.js, `generateAICalligraphyCard`):** 캘리그라피 엽서 생성은 웹폰트 로딩 대기 때문에 최대 5초 정도 걸릴 수 있는데, 그 사이 학생이 도서관 공모전 드로어를 닫고 다른 공모전(예: 픽셀아트)으로 이동해 실제 파일을 업로드하면, 뒤늦게 완성된 캘리그라피 이미지가 공유 변수 `uploadBase64Data`를 덮어써서 엉뚱한(캘리그라피) 이미지가 제출될 수 있었음. 생성 시작 시점의 `activeContest`를 기억해뒀다가, 이미지 생성이 끝나 결과를 반영하기 직전에 여전히 같은 공모전을 보고 있는지 확인하고, 아니면 결과 반영을 건너뛰도록 수정 (성공 경로와 이미지 로드 실패 시의 폴백 경로 둘 다).

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. 레이스 컨디션이라 실제로는 캘리그라피 생성을 누른 직후 바로 다른 공모전으로 이동해 업로드해보며 확인하는 게 가장 확실함.

### 9·10번 항목 수정: 관리자 UI의 미구현 버튼 두 개
관련 항목: **#9, #10**.

**범위 판단:** 두 버튼 모두 "아직 실제로 만들어지지 않은 기능"이라는 게 공통점입니다. 이 코드베이스에는 이미 "준비 중" 안내로 처리하는 기존 관례가 있어서(표 뷰의 "표 뷰는 준비 중입니다" 정적 문구, 미등록 젭퀴즈 4~6회차 링크의 "아직 등록되지 않았습니다 (준비 중)"), 새 기능을 새로 설계해서 만들기보다 그 관례를 따라 **일관되게 "준비 중" 처리**했습니다. 진짜 기능 구현은 별도 작업으로 남겨둠.

**9번 (app.js, `window.openAdminEvalModal`):** 정의 자체가 없어서 클릭 시 콘솔 에러만 나던 함수를 새로 추가 — 클릭하면 "상세 평가 기능은 아직 준비 중입니다" 토스트만 뜨도록 함. 최소한 사용자에게 뭔가 반응은 보여줌.

**10번 (app.js, `setAdminViewMode`):** "표 뷰"로 전환할 때 호출하던 `renderAdminSubmissionsTable()`을 제거. 이 함수는 실제로는 숨겨진 갤러리 컨테이너에 렌더링해서 아무 의미가 없었고, 어차피 index.html에 있는 정적 "표 뷰는 준비 중입니다" 문구만 보였음 — 이제 그 안내만 그대로 보여주고 쓸데없는 렌더링 작업은 생략함.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료.

### 앞으로의 작업 규칙
- 코드를 수정할 때마다 이 문서 하단에 날짜/변경 내용/관련 항목 번호를 추가 기록한다.

---

## 2026-08-15

### 9월 온라인 도서관 — AI 캘리그라피 서체 버그 수정
사용자 요청: "AI 엽서에서 폰트가 제대로 적용되지 않아." 원본 리뷰 항목과는 무관한 별도 이슈.

**진단:** `sub-calli-font` 드롭다운의 7개 서체 중 5개(독도체/나눔붓체/나눔펜체/감자꽃체/연성체)는 구글 폰트로 정상 연결되어 있어 실제 라이브 사이트에서 캔버스에 정상 렌더링되는 것을 직접 확인함. 반면 **"사용자 에셋 서체 1/2"(CustomFont1/CustomFont2)는 `@font-face` 선언이 코드베이스 어디에도 없고 `asset/fonts/` 폴더 자체가 존재하지 않아서, 선택해도 항상 시스템 기본 폰트로 조용히 대체**되고 있었음 (라이브 사이트에서 직접 캔버스 렌더링 테스트로 확인).

**해결 방향 논의:** 실제 폰트 파일을 추가해서 살릴지, 아예 드롭다운에서 제거할지 사용자에게 확인 → **제거하기로 결정**.

**변경 내용 (app.js, 캘리그라피 서체 선택 `<select>`):** "사용자 에셋 서체 1/2" `<option>` 2개와 그 아래 안내 문구("개인 폰트는 asset/fonts/custom1.ttf 경로로...")를 제거. 이제 실제로 작동하는 5개 구글 폰트만 노출됨.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. `CustomFont` 문자열이 코드베이스 어디에도 남아있지 않음을 grep으로 재확인.

### 9월 온라인 도서관 — 캘리그라피 서체 "일부 글자만 다르게 찍히는" 버그 수정 (2차)
사용자가 위 수정 이후에도 문제 스크린샷을 보내옴: 같은 문구("안녕하세요, 반갑습니다") 안에서 일부 글자는 선택한 서체로, 일부는 다른(기본) 서체로 섞여서 렌더링됨.

**원인 재조사:** 라이브 사이트에서 직접 재현 테스트. 기존 코드는 폰트 로딩 완료를 "캔버스에 시험 삼아 글자를 그려서 폭이 시스템 기본 폰트와 달라지는 순간"으로 판단했는데(`document.fonts.load()`는 await 없이 fire-and-forget으로만 호출), 브라우저가 폰트의 가로 폭 정보는 먼저 확정하고 실제 글자 모양(글리프)은 뒤이어 준비하는 경우가 있어 그 타이밍에 캔버스에 그리면 일부 글자만 옛날 폰트로 찍히는 것으로 추정됨. 실제로 `document.fonts.load()`를 제대로 `await`하는 방식으로 같은 문구를 5개 서체 전부 그려보니 전부 깨짐 없이 일관되게 렌더링되는 것을 확인해서 원인을 특정함.

**변경 내용 (app.js, `generateAICalligraphyCard`):** 기존의 "캔버스 텍스트 폭 비교" 방식 폰트 로딩 검증 로직을 전부 제거하고, 브라우저가 직접 보장하는 `document.fonts.load()` + `document.fonts.ready`를 제대로 `await`하는 방식으로 교체 (5초 타임아웃은 유지해서 네트워크 문제로 무한 대기하지 않도록 함).

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. 새 방식(`await document.fonts.load()` + `document.fonts.ready`)으로 5개 서체 전부 라이브 사이트에서 직접 그려서 글자 섞임 없이 일관되게 렌더링되는 것을 스크린샷으로 확인.

### 9월 온라인 도서관 — 캘리그라피 서체 4종 추가
사용자 요청: "다른 폰트를 더 준비할 수 있을까?" 후보 4개를 제시해 전부 선택받음.

**추가한 서체 (구글 폰트):**
- 송명체 (Song Myung) — 붓글씨 캘리그라피 느낌의 명조체
- 푸어스토리체 (Poor Story) — 자연스러운 손글씨
- 개구체 (Gaegu) — 귀엽고 둥근둥근한 손글씨
- 키랑해랑체 (Kirang Haerang) — 장난스러운 붓글씨 느낌

**변경 내용:**
- `index.html`의 구글 폰트 `<link>` 2곳(preload + stylesheet)에 위 4개 폰트 패밀리 추가
- `app.js`의 캘리그라피 서체 선택 `<select>`에 4개 `<option>` 추가 (기존 5개 뒤에)

**검증 상태:** `node --check` 문법 확인 완료. 4개 서체 전부 라이브 사이트에 구글 폰트 링크를 임시로 주입해 실제 로드·렌더링되는지 직접 캔버스에 그려서 스크린샷으로 확인 — 전부 정상, 글자 섞임 없음.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

---

## 2026-08-16

### 9월 온라인 도서관 — 감상 흐름 개편 (디자인 리뷰 후속)
가상 전문가 4인 프로파일로 도서관 페이지를 리뷰한 뒤, 사용자와 논의해 확정한 4단계를 구현.
리뷰 문서: 세션 아티팩트 2건(UX 리뷰 / 갤러리 화면 설계).

**배경 — 가장 큰 발견:** 좋아요가 `localStorage`에만 저장되고 있어서 **학생끼리 서로의 반응이 전혀 보이지 않는 상태**였음. 백엔드 액션 16개를 전부 확인했으나 좋아요 관련 처리가 없었음. "제출 후 갤러리로 보내 서로 감상하게 하자"는 사용자 아이디어가 이 구조에서는 성립할 수 없어서, 서버 저장부터 선행함.

**① 감상 반응 서버 저장 (백엔드 + 클라이언트)**
- 백엔드에 `getReactions` / `toggleReaction` 액션 추가. `Reactions` 시트에 `SubmissionID · StudentUsername · ReactionType · Timestamp` 한 줄씩 저장.
- 반응 종류를 하트 하나에서 **3종(`read` 📖 나도 읽고 싶어 / `art` ✨ 글씨가 멋져 / `heart` 💛 마음에 남아)**으로 분리. 초등학생 대상에서 단일 점수 공개는 인기투표가 되기 때문 (아동 UX 관점 지적).
- **"인기순 정렬"을 의도적으로 삭제** (`index.html`의 select 옵션 + 정렬 로직). 같은 이유.
- `toggleReaction`은 제출과 동일하게 Users 시트 대조로 신원 확인 (`isRegisteredStudent` 헬퍼 신규).
- 클라이언트: `toggleLikePostcard`(localStorage) 전면 제거 → 낙관적 업데이트 + 실패 시 롤백하는 `window.toggleReaction`으로 교체. 서랍 갤러리와 전자화랑에 같은 버튼이 동시에 떠 있을 수 있어 `paintReactionButtons`가 화면 전체의 짝을 함께 갱신함.

**② 갤러리 카드 재구성 (2열 → 1열 피드)**
- 서랍 폭 468px에서 2열이면 엽서가 200px로 줄어 44px로 그린 붓글씨가 **11px**이 되어 읽을 수 없었음. 1열이면 26px로 읽힘.
- **도서명이 화면에 아예 없던 문제도 수정** — `displayTitle`을 계산해놓고 `alt` 속성에만 쓰고 있었음.
- 카드에 글귀(`book-text`) · 도서명 · 저자를 텍스트로 노출. 셋 다 이미 저장 중이던 데이터라 백엔드 변경 불필요.
- 학년만 표시하고 **반은 제거** — 한 반 안에서 `홍*동`은 거의 특정되어 마스킹이 무의미했음.
- 본인 작품에 "내 엽서" 표시 + 제출 직후 해당 카드로 자동 스크롤.
- 학생 입력을 화면에 넣게 되어 `escapeHtml` 헬퍼 신규 추가.

**③ 제출 후 흐름 연결**
- 기존: 제출 성공 → 토스트 → `closeContestDrawer()` (서랍이 그냥 닫힘, 가장 뿌듯한 순간에 아무것도 없음).
- 변경: 도서관 공모전은 서랍을 닫는 대신 **갤러리 탭으로 전환**하고 방금 낸 내 엽서로 스크롤. 다른 공모전은 기존 동작 유지.

**④ 전시관 입구 분리 + 키오스크 모드**
- **접수 상태와 감상 가능 여부를 분리**: 기존엔 갤러리가 공모전 서랍 안에만 있어서, 접수를 닫아두면(현재 운영 방식) 학생이 전시를 볼 방법이 아예 없었음. 메인 페이지에 "독서 엽서 전시관" 진입 섹션을 신설해 접수 상태와 무관하게 열리도록 함.
- **`?did=1` 키오스크 모드** 신설: 로그인 없이 전시가 바로 열리고, 조작 UI(필터·검색·정렬·자동재생 토글·반응 버튼·좌우 화살표)를 숨기며, 글귀를 크게 키우고, 슬라이드 전환을 4초 → **8초**로 늦춤. 복도 스크린엔 이 주소만 즐겨찾기 해두면 됨.
- **3분마다 데이터 자동 갱신** 추가: 기존 자동재생은 슬라이드만 넘기고 서버를 다시 부르지 않아, 아침에 띄워두면 하루 종일 아침 데이터만 돌았음.

**구현 중 함께 고친 버그 2건:**
- 전자화랑 슬라이드쇼가 `entry.data["comment"]`를 읽고 있었으나 실제 저장 필드는 `book-text`라, **항상 "인상 깊은 구절" placeholder만 표시**되고 있었음.
- 도서관 갤러리(1열)를 본 뒤 키링 갤러리로 넘어가면 1열 배치가 남는 문제 → `renderGallery2025`에서 클래스 제거.

**검증 상태:** `node --check` (전체 파일 + 추출한 백엔드 블록) 통과. 로컬 서버로 띄워 실제 렌더링 확인 — 카드 레이아웃·반응 버튼 on/off 상태·전시관 진입 섹션·키오스크 모드(조작 UI 숨김, 자동재생 활성) 전부 스크린샷으로 확인. 라이브 백엔드에 도서관 제출작이 아직 0건이라 실데이터 렌더링은 미확인.

**✅ 배포 및 검증 완료 (2026-08-16):** Apps Script 재배포 후 curl 왕복 테스트 — 새 액션 응답 정상, 미가입 계정·잘못된 반응 종류 차단, 반응 저장 → **다른 학생 계정에서도 카운트 보임(`mine`은 비어있음)** → 취소 → 시트 깨끗함. 테스트 흔적 없음. Vercel 배포 후 라이브에서 새 코드·전시관 입구·`?did=1` 키오스크 모드 동작 확인.

### 9월 온라인 도서관 — 일정 변경 및 접수 오픈
사용자 요청: 일정을 2026.8.24.(월) ~ 2026.9.30.(수)로 수정하고 지금 오픈.

**일정:** `CONTESTS_DATA`의 `library.period`를 `2026. 8. 24.(월) ~ 2026. 9. 30.(수)`로 변경.

**오픈 방식 — 하드코딩 대신 죽어 있던 잠금 장치를 살림:**
조사해보니 Settings 시트에 **이미 `contest_lock_library: true`(잠김)가 저장되어 있었고**, `fetchContestLocks()`가 이 값을 `contestLocks` 전역에 가져오기까지 했지만 **어디서도 읽지 않고 있었습니다** (최초 리뷰 3번 항목에서 지적했던 죽은 코드). "library를 강제로 열기"를 코드에 박아넣는 대신, 원래 있던 이 장치를 양쪽에 연결했습니다.

- **클라이언트** `getContestStatus()`: 젭퀴즈가 아닌 공모전은 `contestLocks[contestId] === false`일 때만 `"active"`. 잠금 정보가 없으면 안전하게 마감으로 처리.
- **백엔드** `submitContest`: 비-젭퀴즈를 일괄 차단하던 것을 `isContestLocked(ss, contestId)` 헬퍼로 교체해 같은 Settings 값을 보게 함. 클라이언트만 열어도 서버가 거부하던 불일치를 해소.
- **관리자 UI 신설**: 관리자 대시보드 "공모전 관리" 탭에 접수 상태 칩 목록 추가. 클릭 한 번으로 열고 닫히며, 지금까지 **호출하는 곳이 전혀 없던 `updateContestLock` 액션**을 여기에 연결. 이제 공모전 개폐가 코드 수정이 아니라 버튼 조작이 됨.
- 선언만 되어 있고 아무 데서도 쓰이지 않던 `FORCE_ACTIVE_CONTESTS` 삭제.

**검증 상태:** `node --check` 통과. 로컬 서버에서 잠금 칩 UI 렌더링 확인 (도서관만 초록 "접수중", 나머지 "마감").

**✅ 배포 및 검증 완료 (2026-08-19):** Apps Script 재배포 후 curl 확인 — 잠긴 도서관 제출 차단, 관리자 토큰 없는 잠금 변경 차단, 무단 변경 시도 후에도 잠금 상태 유지. Vercel 배포 후 라이브에 새 코드 반영 확인. **접수를 실제로 여는 버튼은 관리자 비밀번호가 필요해 사용자가 직접 눌러야 함.**

### 로그인 속도 개선 (왕복 3회 → 1회)
사용자 보고: "사이트 로그인이 너무 느려."

**측정:** Apps Script 웹앱은 302 리다이렉트를 거치는 구조라 **왕복 1회에 2.5~4초**가 걸림(3회 측정: 2.87s / 2.57s / 4.12s). 이게 줄일 수 없는 바닥값.

**원인:** `handleLogin`이 예전 평문 비밀번호 계정을 처리하려고 **순차적으로 3번 왕복**하고 있었음.
1. 해시로 로그인 시도 → 실패
2. 평문으로 재시도 → 성공
3. `signUp` 액션으로 해시 재등록 (이것도 `await`)

즉 아직 해시로 전환되지 않은 계정은 로그인에 **9~12초**가 걸렸음. (한 번 로그인하면 전환되어 이후엔 1회로 빨라지지만, 전환 안 된 학생이 많으면 첫 로그인마다 이 비용을 치름.)

**해결 — 서버가 한 번에 처리하도록 변경:**
- Apps Script에 `sha256Hex()` 헬퍼 추가. 클라이언트 `hashPassword()`와 동일한 SHA-256 소문자 16진수를 만듦.
- `login` 액션이 저장된 값과 받은 해시를 비교하고, 안 맞으면 **저장된 평문을 서버에서 직접 해시해서** 대조. 맞으면 그 자리에서 셀을 해시로 교체.
- 클라이언트는 해시 1개만 보내고 왕복 1회로 종료. 평문 재시도·재가입 요청 전부 삭제.

**부수 효과:** 예전에는 폴백 과정에서 **평문 비밀번호가 네트워크로 전송**됐는데, 이제 어떤 경우에도 해시만 전송됩니다.

**예상 개선:** 미전환 계정 기준 약 9~12초 → 약 3초.

**검증 상태:** `node --check` 통과(전체 + 백엔드 블록). 해시 형식 일치는 코드 레벨로 확인(양쪽 다 SHA-256 소문자 16진수 64자). **실제 로그인 동작은 비밀번호가 필요해 사용자 확인 필요.**

**⚠️ 배포 순서 주의 — 반드시 백엔드 먼저:**
새 클라이언트는 평문 폴백을 하지 않으므로, **프론트엔드를 먼저 배포하면 아직 해시로 전환되지 않은 계정이 로그인 불가**가 됩니다. 반드시 `Apps Script 재배포` → 확인 → `main 병합(프론트 배포)` 순서로 진행할 것.

**✅ 배포 완료 (2026-08-19):** 백엔드 먼저 배포 후 프론트엔드 병합(PR #13). 라이브에서 폴백 코드 제거 확인.

### 중복 서버 호출 제거 (Apps Script 구조 유지한 채 최적화)
사용자 요청: Firebase 이전 대신 현재 구조에서 최적화.

**전제 — 측정으로 확인한 사실:** Apps Script 호출 1회가 **2.7~8.4초**(편차 큼). 이건 302 리다이렉트를 거치는 구조 탓이라 줄일 수 없음. 따라서 최적화 방향은 **"호출 자체를 줄이는 것"** 하나뿐.

**찾아낸 낭비 두 가지:**

1. **`getSubmissions`(내 제출 내역)를 5곳에서 각각 따로 호출** — 공모전 서랍 열 때, 제출 취소 전 사은품 확인할 때, 픽셀아트 에디터 열 때, 내 내역 조회할 때, 카운터 갱신할 때. 전부 같은 데이터인데 매번 3~8초씩 다시 받아옴.
2. **갤러리·전자화랑의 학년 필터·검색·정렬이 매번 서버를 다시 호출** — 정작 필터링·검색·정렬 로직은 데이터를 받은 뒤 클라이언트에서 처리하는데도, 필터 칩 한 번 누를 때마다 전체 목록을 다시 받아오고 있었음.

**해결:**
- `getMySubmissions()` 캐시 함수 신설. 5개 호출부를 전부 여기로 통일. 진행 중인 요청이 있으면 함께 기다려 중복 요청을 막고(`_mySubmissionsPromise`), 다른 계정으로 로그인하면 캐시를 폐기.
- `fetchLibrarySubmissionsRaw()`로 목록 조회와 필터링을 분리. 목록은 한 번만 받아 캐시에 두고, 필터·검색·정렬은 캐시본을 다시 거르기만 함.
- 제출·취소 시점에 두 캐시를 모두 무효화. 로그아웃 시에도 내 내역 캐시 폐기.
- 키오스크 3분 자동 갱신은 캐시를 건너뛰도록 `forceRefresh` 지정.

**함께 정리한 것:**
- `updateLiveCounters()`의 로그아웃 레이스 수정 — 조회 중 로그아웃하면 늦게 도착한 응답이 "0개"를 이전 사용자 숫자로 덮어쓰던 문제. 요청 시점 계정을 기억해뒀다가 바뀌었으면 화면에 쓰지 않음.
- 쓰기만 하고 아무도 읽지 않던 `GOOGLE_SHEET_API_URL_error` 변수 제거 (최초 리뷰에서 지적됐던 죽은 코드).

**검증 상태:** `node --check` 통과. 브라우저에서 `fetch`를 가로채 실제 호출 수를 세어 확인 —
- 갤러리 필터 4번 변경 → 서버 호출 **1회** (기존 4회)
- `getMySubmissions()` 동시 3회 → 서버 호출 **1회**, 이후 반복 호출 → **0회**
- 계정 변경 시 → 정상적으로 재조회
- 강제 새로고침 → 정상적으로 재조회

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

#### 후속 수정: 캐시 때문에 새 작품이 안 보이던 문제
사용자 보고: "전자화랑이 제대로 작동하지 않아. 학생들 작품이 다 보이지 않아."

**원인 — 바로 위 캐시 최적화가 만든 회귀입니다.** 전시관·갤러리를 *여는* 순간에도 캐시를 재사용해서, **세션 중에 새로 들어온 제출작이 화면에 반영되지 않았습니다.** 선생님이 전시관을 한 번 열어두면 그 뒤 학생이 제출한 작품은 새로고침 전까지 안 보이는 상태였음.

**조사 과정:** 서버 데이터를 직접 확인한 결과 제출 12건이 정상 저장되어 있었고, 학생당 중복 제거·이미지 유효성 필터 어느 단계에서도 걸러지지 않아 12건 전부 표시되는 게 맞았음 → 데이터 문제가 아니라 캐시 문제로 특정.

**수정:** "여는 동작"과 "필터 바꾸는 동작"을 구분.
- `openDIDExhibition()` / 갤러리 탭 열기 → `forceRefresh`로 항상 새로 조회
- 학년 필터·검색·정렬 변경 → 캐시 사용 (즉시 반응 유지)

최적화 효과는 그대로 두고 신선도만 회복했습니다.

**검증:** 브라우저에서 호출 수를 세어 확인 — 전시관 열기 1회 조회 → 필터 3번 변경해도 추가 조회 0회 → 다시 열면 1회 추가 조회. 실제 화면에 학생 12명 작품이 전부 렌더링되고 엽서 이미지도 정상 로드되는 것을 스크린샷으로 확인.

#### 클릭 확대 화면이 작던 문제 + 자동 재생 속도
사용자 요청: "회랑에서 클릭했을 때 조금 더 큰 화면이 나왔으면", "자동 재생 속도가 너무 빠른데 지금 몇 초야?" → 8초로 결정.

**확대 화면이 작던 원인:** 갤러리 카드가 쓰는 구글 드라이브 축소본 주소(`sz=w600`)를 클릭 확대에도 그대로 넘기고 있었음. 실제로 확인해보니 `sz=w600`은 **600×450으로 줄어든 이미지**를 주고, `sz=w1200` 이상을 요청하면 **원본 800×600**이 옴. 게다가 모달이 `max-width: 90%`만 지정해서 원본보다 크게 늘어나지 않아, 1440px 화면에서 800px(화면의 56%)로 작게 떴음.

**수정:**
- `getGoogleDriveFullLink()` 추가 (`sz=w1600`). 목록·카드는 가벼운 600px 축소본을 그대로 쓰고, **클릭 확대할 때만 원본**을 받아옴.
- 모달 이미지를 `width: min(92vw, calc(92dvh * 4 / 3))`로 지정해 비율을 유지한 채 화면에 맞춰 커지도록 변경.
- 결과: 1440×900 화면에서 **800×600 → 1104×828** (화면 가로의 77%).

**자동 재생 속도:** 4초 → **8초**. 기존에는 일반 전시관 4초 / 키오스크 8초로 나뉘어 있었는데, 글귀 한 줄과 도서명을 읽기엔 4초가 짧아 8초로 통일.

**검증:** 로컬에서 확인 — 카드는 `sz=w600`, 모달은 `sz=w1600`으로 원본 800×600을 받아오고 화면에 1104×828로 표시되는 것과, 실제 엽서가 크게 보이는 화면을 스크린샷으로 확인.

---

## 2026-08-21

### 동시 접속 문제 진단 및 Firebase 이전 계획 수립
사용자 보고: "여러명이 동시에 접속하려면 시간이 오래 걸려."

**측정 (실제 백엔드에 동시 요청을 보내 확인):**

| 동시 접속 | 가장 빠른 사람 | 중간값 | 가장 오래 기다린 사람 |
|---|---|---|---|
| 1명 | 3.0초 | 3.0초 | 3.0초 |
| 5명 | 2.3초 | 2.3초 | 2.6초 |
| 10명 | 6.2초 | 6.2초 | 6.3초 |
| **25명(한 학급)** | 6.0초 | 6.5초 | **64.2초** |

간헐적 실패는 아님(20회 연속 호출 전부 성공). **전형적인 대기열 현상.**

**원인:** Apps Script 웹앱이 "소유자로 실행"되어 **모든 학생 요청이 계정 하나로 순차 처리**됨. 스프레드시트 접근도 동시 쓰기를 막느라 직렬화됨. 로그인을 3회→1회 왕복으로 줄인 작업은 각자의 처리 시간을 줄인 것이지 줄 자체를 없애지는 못함. **코드 최적화로 넘을 수 있는 한계에 도달.**

**결정:** 사용자와 논의 후 Firebase 이전 계획 수립. 계획서는 세션 아티팩트로 작성.

**이전 시 데이터 규모(실측):** 제출물 약 873건(키링 131·컷툰 125·젭퀴즈 604·도서관 12·픽셀아트 1), 이미지 있는 제출물 약 269건, 학생 계정 약 250~300명. Firestore 무료 한도에 한참 못 미침 — **데이터 양은 걸림돌이 아님.**

**사용자가 수용한 두 가지 비용:**
1. **스프레드시트 직접 편집 상실** — 조회는 관리자 대시보드 + 내보내기로 대체 가능하나, 시트에서 고치면 반영되는 흐름은 포기. (사용자 확인: "괜찮아")
2. **기존 비밀번호 이전 가능 여부 불확실** — 솔트 없는 SHA-256이라 Firebase Auth 가져오기가 통할지 검증 필요. 안 되면 전원 재설정. (사용자 확인: "알았어")

**이미지는 옮기지 않기로 결정:** Firebase Storage는 결제 계정 등록이 필요한데, 이미지는 제출물에 URL 문자열로만 들어있어 드라이브에 둬도 전부 동작하고 속도 문제와도 무관함. Firestore+Auth만 쓰면 카드 없이 무료 한도 내에서 해결됨.

### 1단계 도구 작성: 비밀번호 이전 검증 (`migration/`)
가장 불확실한 항목을 맨 앞에서 확인하기 위한 도구. **실제 학생 데이터를 쓰지 않고 가짜 계정 1개로 검증**하므로 위험 없음.

- `migration/make-import-file.js` — 우리 해시(소문자 16진수)를 Firebase가 요구하는 base64로 변환해 `auth:import` 파일 생성. `--test` 옵션으로 비밀번호를 아는 가짜 계정 생성.
- `migration/login-test.html` — 가져온 계정이 **원래 비밀번호로 실제 로그인되는지** 확인하는 최소 페이지. 실패 시 오류 코드별로 다음 조치를 안내.
- `migration/README.md` — 실행 순서와 판정 기준.

**합성 이메일 설계:** 학생은 학년/반/번호/이름으로 로그인하는데 Firebase Auth는 이메일을 요구함. 이름에 한글이 들어가면 이메일로 쓸 수 없어, `userKey`를 SHA-256 해시한 ASCII 주소(`u30ec1130...@soro.local`)를 쓰기로 함. 학생은 이 주소를 볼 일이 없고 앱이 같은 규칙으로 재생성.

**검증 상태:** 해시 변환을 역검증으로 확인 (16진수 → base64 → 16진수 왕복 일치, 32바이트). `node --check` 통과. **실제 Firebase 로그인 검증은 프로젝트 생성이 필요해 사용자가 직접 실행해야 함.**

**일정 합의:** 1~3단계(검증·설계·데이터 복사)는 학생 화면·원본 데이터를 건드리지 않아 지금 진행 가능. **실제 전환(4~6단계)은 도서관 공모전이 끝나는 10월 이후.** 그때까지 동시 접속은 학급별 시차 운영으로 대응(5명 동시는 2.3초로 정상).

### Firebase 이전 1단계 검증 완료 — 기존 비밀번호 이전 가능 확인 (2026-08-22)

**결과: ✅ 통과. 학생 전원 비밀번호 재설정은 필요 없습니다.**

계획서에서 "가장 불확실하고 일정을 좌우하는 항목"으로 지목했던 부분입니다.

**처음 실패한 원인:** `firebase auth:import` 명령에서 **`--hash-input-order` 플래그 누락**. SHA 계열은 저장된 해시를 `SHA(비밀번호+솔트)`로 볼지 `SHA(솔트+비밀번호)`로 볼지를 이 플래그로 판단하는데, 지정하지 않으면 **업로드는 성공(`Imported successfully`)하지만 로그인은 실패**합니다. 사용자가 겪은 증상과 정확히 일치.

**확정된 조합** (3단계 실제 이전에도 그대로 사용):
```
--hash-algo=SHA256  --rounds=1  --hash-input-order=PASSWORD_FIRST
```
- `passwordHash`: 우리 16진수 해시를 **표준 base64**로 변환
- `salt`: **필드 자체를 넣지 않음** (빈 문자열로 넣으면 다르게 해석될 여지가 있음)

**검증 방법:** 비밀번호를 아는 가짜 계정(`soro-test-1234`) 1개를 시험용 프로젝트 `soro-migration-test`에 가져온 뒤, Identity Toolkit REST API로 **원래 비밀번호로 로그인 시도** → `registered: true` 및 idToken 정상 발급 확인. **실제 학생 데이터는 사용하지 않음.**

**작업 분담 기록:** 구글 계정 로그인(`firebase login`)과 프로젝트 생성은 계정 인증이라 사용자가 직접 수행. 그 이후 단계(가져오기 파일 생성 → import → 로그인 검증)는 Claude가 실행.

**부수 정리:** 전역 `npm install -g firebase-tools`가 권한 문제로 막혀서 모든 명령을 `npx --yes firebase-tools@latest ...` 방식으로 변경.

**다음:** 2단계(Firestore 구조 및 보안 규칙 설계). 실제 전환(4~6단계)은 예정대로 도서관 공모전 종료 후.

### Firebase 이전 2단계: Firestore 구조 및 보안 규칙 설계 (2026-08-22)

시트 4장 → 컬렉션 4개 설계. 상세는 [migration/DESIGN.md](migration/DESIGN.md), 규칙 본문은 [migration/firestore.rules](migration/firestore.rules).

**문서 ID를 고정한 것이 핵심 설계 결정:**
- `submissions/{contestId}__{uid}` — 한 학생당 한 공모전 하나가 DB 차원에서 보장되고, **재제출해도 같은 문서를 덮어써서 받은 반응이 사라지지 않음**(현재는 취소 후 재제출 시 id가 바뀌어 반응 소실 — 최초 리뷰에서 지적했던 문제가 구조적으로 해결됨). 내 제출물 조회도 질의 없이 문서 직접 접근으로 처리됨.
- `reactions/{submissionId}__{uid}__{type}` — 같은 사람이 같은 작품에 같은 반응을 두 번 남기는 것이 구조적으로 불가능해짐.

**규칙이 강제하는 것 (지금은 서버 코드로 손수 검사하던 것들):**
- **실존 학생 사칭 차단** — `request.auth.uid`가 아니면 문서를 쓸 수 없음. **이번 이전으로 해결되는 가장 큰 항목** (4번 항목의 "알려진 한계"로 남겨뒀던 그것).
- 마감된 공모전 제출 차단 — 규칙이 `settings` 문서를 직접 읽어 판단.
- 학생이 `isStarred`/`prizeStatus`/`award`를 스스로 붙이는 것 차단 (현재는 검사 자체가 없어 보내면 저장됨).
- 자기 작품에 반응하는 것 차단.
- 관리자 판별을 하드코딩된 `5_1_27_김태호` 값 비교에서 **Auth 커스텀 클레임**으로 전환 — 담당자가 바뀌어도 코드 수정 불필요.

**결정을 미룬 것:** `submissions` 공개 읽기라 주소만 알면 학생 이름이 그대로 보임. 다만 **현재 Apps Script도 동일**(`getAllSubmissions`가 전체 이름 반환)하므로 이번 이전으로 나빠지지는 않음. 더 가리려면 마스킹된 공개용 사본 컬렉션이 필요하고 쓰기가 두 곳으로 늘어남 → 4단계에서 실제 화면 보며 판단하기로 함.

**⚠️ 검증 미완료:** 규칙을 실제 배포해 시험하지 못함. 시험 프로젝트에 Firestore API가 비활성 상태라 DB 생성이 거부됐고(403), 로컬 에뮬레이터는 이 컴퓨터에 Java가 없어 실행 불가. 현재는 문법·괄호 균형·JSON 유효성만 확인한 상태. **사용자가 Firebase 콘솔에서 Firestore 데이터베이스를 생성해주면**(위치 `asia-northeast3`, 프로덕션 모드) 배포 후 실제 권한 시험 예정.

**✅ 2단계 규칙 검증 완료 (2026-08-22):** 시험 프로젝트에 Firestore DB 생성(사용자) 후 규칙·색인 배포. 가짜 학생 계정 2개로 실제 요청을 보내 **10개 시험 전부 통과**. 재현: `node migration/rules-test.mjs`.

- **정상 동작 확인(4건):** 본인 제출, 재제출(덮어쓰기), 남의 작품에 반응, 자기 반응 취소. *— 막는 것만 시험하면 "전부 막는 규칙"도 통과하므로 긍정 케이스를 반드시 함께 확인함.*
- **차단 확인(6건):** **타인 사칭 제출**, 로그인 없이 제출, 잠긴 공모전 제출, 남의 작품 삭제, 학생이 수상 표시 부착, 자기 작품에 반응, 반응 위조, 미허용 반응 종류, 학생의 설정 변경.

**사칭 차단이 실제로 동작하는 것을 확인한 것이 핵심** — 4번 항목에 "이 구조로는 못 막음"이라고 한계로 명시했던 문제가 해결됨.

시험 준비 과정에서 `settings` 문서를 심기 위해 해당 컬렉션만 임시로 여는 규칙을 잠깐 배포했다가 **원래 규칙으로 복구 후 시험**함. 위 결과는 전부 정식 규칙 적용 상태에서 나온 값.

### 라이브 버그 발견 및 수정: 제출 시각이 파싱되지 않아 정렬·중복제거가 무력화됨 (2026-08-22)

3단계 데이터 내보내기 중 발견. **이전 작업과 무관한, 지금 운영 중인 버그입니다.**

**증상:** 제출 시각을 `new Date().toLocaleString("ko-KR")`로 저장해서 `"2026. 8. 19. 오후 2:02:00"` 형태인데, 이 문자열은 `new Date()`가 파싱하지 못해 **Invalid Date**가 됩니다. 실제 데이터 **882건 전부 파싱 실패** 확인.

**영향 (3곳):**
- `getLibrarySubmissions` 최신순 정렬 — `NaN` 비교라 정렬이 아예 이뤄지지 않음
- 도서관 갤러리 "학생당 최신 1건" 중복 제거 — 비교가 항상 거짓이라 **최신이 아니라 시트에서 먼저 나온 것**이 남음
- 키링 명예의 전당 캐러셀 중복 제거 — 위와 동일

즉 갤러리가 "최신순"이라고 표시하면서 실제로는 정렬하지 않았고, 재제출한 학생은 옛 작품이 노출되고 있었을 수 있음.

**수정:** `parseSubmissionTime()` 헬퍼 추가. 한국어 로케일 문자열을 직접 해석하고, ISO 등 표준 형식도 대비. 3곳의 `new Date(...)` 비교를 전부 교체.

**검증:** 실제 내보낸 882건 전부 파싱 성공(실패 0건). 경계 케이스 확인 — `오후 12시`=정오, `오전 12시`=자정으로 올바르게 해석됨(KST→UTC 9시간 차 확인). 도서관 갤러리 정렬 결과가 실제 최신순으로 나오는 것 확인.

저장 형식은 그대로 둡니다(화면에 그대로 표시되는 값이라 사람이 읽기 좋음). 파서가 양쪽을 모두 처리합니다.

### Firebase 이전 3단계: 데이터 옮기기 (2026-08-22)

**시험 프로젝트로의 이전을 끝까지 수행하고 검증 완료.** 원본 스프레드시트·Apps Script는 전혀 건드리지 않음(읽기 전용).

**도구 4개 추가 (`migration/`):**
- `export-from-sheets.mjs` — 현재 백엔드에서 읽기 전용 내보내기. 관리자 토큰이 필요한 `contestId:"all"` 대신 **공모전별 공개 조회를 조합**해 우회.
- `transform.mjs` — CSV·JSON을 Firebase 형식으로 변환.
- `load-to-firestore.mjs` — Firestore 적재(묶음 처리·재시도 포함).
- `verify.mjs` — 원본 대비 건수·내용 대조.

**사용자 계정 (CSV 로 받음):**
- 483행 → **고유 479명** (중복 4건은 비밀번호까지 동일한 단순 중복이라 통합)
- **평문으로 남아있던 12건을 해시로 전환**해서 이전 (원래는 다음 로그인 때 자동 전환되는데, 이전 시점에 미리 맞춤)
- Auth 가져오기 479명 성공

**제출물:**
- 내보낸 882건 → **문서 870개** (같은 학생·같은 공모전 중복 12건이 최신 1건으로 통합)
- 문서 ID를 `contestId__uid`로 고정한 설계가 그대로 적용됨

**명단에 없는 학생 4명의 작품 14건 처리 (사용자 확인 후 결정):** 계정만 삭제되고 작품은 남은 경우로, **현재 갤러리에 실제로 보이는 작품들**(키링·컷툰 포함). 그냥 버리면 전시에서 사라지므로, 제출물에 들어있는 학년·반·번호·이름으로 **프로필만 만들어 작품을 보존**하기로 함(`inactive: true`, 비밀번호 없어 로그인 불가). → Firestore 프로필 483명 = 로그인 가능 479 + 전시 전용 4.

**검증 결과:**
- 제출물 870 / 870 ✅ · 사용자 483 / 483 ✅ · 문서 ID 중복 0 ✅
- 내용 표본 대조(이름·도서명·이미지 URL) 일치 ✅
- **평문이었다가 해시로 전환한 실제 학생 계정으로 원래 비밀번호 로그인 성공** ✅
- 적재를 위해 임시로 열었던 규칙을 **원래 보안 규칙으로 복구** 후 재시험 → 10/10 통과 ✅

**규칙 시험 보완:** 실제 설정을 이전하면서 `contest_lock_library`가 `true`(마감)가 되자 "열린 공모전 제출" 시험 2건이 실패로 나옴. 규칙 결함이 아니라 시험이 특정 상태를 전제한 탓이라, **현재 잠금 상태를 읽어 기대값을 맞추도록** 시험을 수정.

**아직 못 옮긴 것 — 감상 반응:** 공개 `getReactions`는 작품별 **합계만** 돌려주고 "누가 눌렀는지"는 주지 않아, 새 구조(`reactions/{제출물}__{uid}__{종류}`)로 옮길 수 없음. **Reactions 시트 CSV가 필요.** 참고로 현재 반응 63개 중 **14개(22%)는 이미 대상 작품이 없어**(삭제·재제출로 id 변경) 어차피 옮길 수 없음 — 최초 리뷰에서 지적한 "재제출 시 반응 소실"이 실제 데이터로 확인된 것이며, 새 설계는 이를 구조적으로 막음.

**보안:** 실제 학생 데이터가 담긴 `export/`·`build/`·`*.csv`는 `.gitignore`로 커밋 제외.

**감상 반응 이전 완료 (Reactions CSV 수령 후):**

원본 63행 → **문서 39개**. 사라진 24건의 내역:
- **17건** — 대상 작품이 이미 없음(삭제되었거나 재제출로 id 변경). 최초 리뷰에서 지적한 "재제출 시 반응 소실"이 실제 데이터로 나타난 것.
- **6건** — 자기 작품에 남긴 반응. 2단계 규칙에서 금지하기로 한 항목이라 제외. *(이 정책을 바꾸고 싶으면 규칙과 함께 되살릴 수 있음)*
- **1건** — 완전히 동일한 중복 행. 새 구조(`reactions/{제출물}__{uid}__{종류}`)에서는 애초에 생길 수 없음.

**검증:** 반응 39/39 ✅ · 끊어진 반응 0 ✅ · 자기 작품 반응 0 ✅

**시험 도중 발견한 두 가지:**
1. **규칙 배포 후 즉시 시험하면 전파가 안 끝나 결과가 틀립니다.** 배포 직후 실행에서 6/10이 나왔다가 잠시 뒤 재실행하니 10/10. 규칙 결함이 아니라 전파 지연이었음.
2. 그 전파 지연 구간에서 "거부 기대"였던 쓰기가 통과해 **시험 잔여물 1건이 DB에 남았습니다**(`...__evil`). 검증에서 반응 수가 40/39로 어긋나 발견. 시험이 만들 수 있는 문서를 끝에 전부 지우도록 보강했고, 연속 2회 실행 후에도 39/39 유지되는 것을 확인.

**3단계 최종 상태:** 사용자 483 · 제출물 870 · 반응 39 · 설정 6 — 전부 시험 프로젝트에 적재 및 검증 완료. 정식 보안 규칙 적용 상태에서 10/10 통과.

### Firebase 이전 4단계: 앱이 Firestore를 보도록 교체 (2026-08-22)

**⚠️ 계획상 4단계는 공모전 종료 후로 잡았던 구간입니다.** 도서관 공모전이 진행 중이므로, **기본값은 기존 Apps Script 로 두고 주소로만 전환되는 스위치** 방식으로 구현했습니다. 운영에는 영향이 없고, 5단계에서 한 학급만 새 백엔드로 시험할 수 있습니다.

**구조 — 호출을 한 곳으로 모음:**
클라이언트의 `fetch(GOOGLE_SHEET_API_URL, ...)` **26곳을 전부 `callBackend(payload)` 하나로 치환**(스크립트로 기계적 변환, 응답 사용 25곳 + fire-and-forget 1곳). 액션 이름과 응답 모양(`{status, data, ...}`)을 그대로 유지해서 **화면 코드는 손대지 않고 백엔드만 갈아끼울 수 있게** 했습니다.

- `BACKEND_MODE` — `?backend=firebase` / `?backend=sheets` 로 전환, localStorage 에 유지. **기본값 `sheets`.**
- `firebase-backend.js` 신설 — Apps Script 액션 인터페이스를 그대로 흉내 내는 Firestore 구현.
- `index.html` — firebase 모드일 때만 모듈을 내려받음. **평소에는 다운로드조차 하지 않아 기존 사용자에게 영향 없음.**

**구현 중 발견한 결함 (구조상 로그인이 불가능했던 문제):**
`handleLogin`은 비밀번호를 **SHA-256 해시로 바꿔서** 보내는데(Apps Script 는 해시를 저장·대조), **Firebase Auth 는 원문 비밀번호로 인증**합니다. 해시를 비밀번호로 넘기면 `SHA256(해시)` 와 저장된 `SHA256(원문)` 을 비교하게 되어 항상 실패합니다. → payload 에 `_rawPassword` 를 함께 실어 보내고, **밑줄로 시작하는 항목은 Apps Script 로 전송할 때 제거**하도록 `callBackend` 에서 걸러냅니다. 평문이 기존 백엔드로 나가지 않는 성질은 그대로 유지됩니다.

**검증 (로컬에서 실제 실행):**
- 기본 모드: `sheets`, Firebase 모듈 미로드, 기존 백엔드 정상 응답 ✅
- firebase 모드: 잠금 조회·작품 조회·반응 조회 전부 정상 응답 ✅
- **전자화랑이 Firestore 데이터로 정상 렌더링** — 글귀·도서명·저자·학년·반응 수 표시, 이미지 로드 실패 0 ✅
- **속도 비교(같은 조회 3회 평균): Apps Script 2,330ms → Firestore 232ms — 약 10배** ✅
- 모드 되돌린 뒤 기존 백엔드로 정상 복귀 확인 ✅

**아직 안 옮긴 것 (5·6단계 대상):**
- 관리자 전용 액션 일부: `getZepQuizStats`, `deleteUser`, `updateClassPrizeStatus`, `updateClassStudentCount`, `resetPassword` — 호출 시 "아직 옮기지 않은 기능입니다" 를 돌려주고 콘솔에 경고를 남깁니다.
- 관리자 커스텀 클레임을 아직 아무 계정에도 부여하지 않음 → firebase 모드에서는 관리자 기능이 동작하지 않습니다.
- `firebase-backend.js` 의 설정이 **시험 프로젝트**를 가리킵니다. 6단계에서 운영 프로젝트 값으로 교체 필요.
- 옛 제출물 id(`library_1787...`)로 삭제를 요청하는 경로가 남아 있어, 새 구조(`contestId__uid`)와 대응시키는 보정 코드가 들어가 있습니다. 전환이 끝나면 정리 대상입니다.

### Firebase 이전 5단계 준비: 관리자 기능 완성 및 최신 데이터 재이전 (2026-08-26)

사용자 요청으로 **계획을 앞당겨** 진행. (원래 4~6단계는 도서관 공모전 종료 후로 잡았음)

**관리자 판별 방식 변경 — 커스텀 클레임 → Firestore 문서**
기존 규칙은 `request.auth.token.admin`(Auth 커스텀 클레임)을 봤는데, 클레임을 붙이려면 **서비스 계정 키**가 필요합니다. 학교 운영 환경에서 그 키를 관리하는 부담이 크고 Claude 가 다룰 자격증명도 아니라, `users/{uid}.admin` 문서 값을 읽는 방식으로 바꿨습니다. 학생이 자기 문서에 `admin` 을 붙이는 것은 기존 users 규칙이 이미 막고 있어 보안 수준은 동일합니다.

**미구현이던 관리자 액션 5개 구현** (`firebase-backend.js`):
- `getZepQuizStats` — 학급별 참여 현황 집계. 전시 전용(탈퇴) 계정은 참여율에서 제외.
- `deleteUser` — 제출물·프로필 삭제. **Auth 계정 자체는 관리자 SDK 가 있어야 지워져서, 그 사실을 응답 메시지로 명시**하고 콘솔에서 지우도록 안내.
- `updateClassPrizeStatus`, `updateClassStudentCount` — 그대로 이전.
- `resetPassword` — **이전 불가로 판단**. 현재는 "로그인 없이 비밀번호 재설정"인데 Firebase Auth 는 본인 세션이나 관리자 SDK 를 요구합니다. 별도 설계가 필요해 안내 메시지를 돌려주도록 함.

**관리자 권한 검증 (6/6 통과):** 관리자 계정(`5_1_27_김태호`, uid `9009084b…`)에 `admin: true` 부여. **같은 동작을 관리자와 학생이 각각 시도**해 비교 — 공모전 잠금·활성 회차·학급 정원 모두 관리자는 허용, 학생은 거부. 시험용으로 임시 부여했던 권한을 회수한 뒤 실제로 거부되는 것까지 확인.

**최신 데이터 재이전:** 제출물 882 → **889건**(7건 추가), 문서 877개. 반응 39개. 사용자 484명.
- **재이전 시 삭제 반영 문제**를 발견해 `reconcile.mjs` 추가 — 적재가 덮어쓰기라서 원본에서 삭제된 작품이 Firestore 에 남습니다. 대조 결과 이번엔 0건이었으나, 실제 전환 때는 반드시 돌려야 합니다.
- 관리자 시험에 쓴 계정 프로필 2개를 정리해 484명으로 맞춤.

**⚠️ 실제 전환(6단계)을 막는 것 — 데이터가 계속 움직임:**
Users CSV 가 8/22자라 **그 뒤 가입한 학생 1명(4학년 2반 3번)이 로그인 불가** 상태로 이전됩니다(작품은 보존되나 계정이 없음). 유령 프로필이 4명 → 5명으로 늘어난 것이 그 증거입니다. 공모전이 진행 중인 한 이 격차는 계속 벌어집니다.
→ **전환 시점에 접수를 잠시 닫고(약 15분) 최종 내보내기·적재 후 전환**하는 것이 유일하게 안전한 방법입니다.

**작업 중 반복 관찰:** Firestore 규칙 배포 직후 곧바로 검증하면 결과가 틀립니다(제출물 0건 또는 600건으로 나왔다가 45초 뒤 877건 정상). 규칙·데이터 전파에 시간이 걸리므로 **배포 후 충분히 기다린 뒤 검증**해야 합니다.

### 전환 준비 완료 — 최종 데이터 적재 및 검증 (2026-08-26)

접수를 잠근 상태(사용자가 수행)에서 최신 CSV 2종을 받아 최종 이전을 마쳤습니다.

**최종 수치:** 제출물 **877** · 계정 **484**(로그인 가능 480 + 전시 전용 4) · 반응 **75** · 설정 6 — 전부 검증 통과, 규칙 시험 10/10.

**8/22 명단 대비 변화:** 계정 1명 추가(4학년 2반 3번) — 8/25에 작품을 내고 반응까지 남긴 학생으로, 옛 명단으로 전환했다면 **로그인 불가 상태가 될 뻔했습니다.** 이것이 "전환 시점에 최신 명단이 필요하다"는 근거였고 실제로 확인됐습니다. 나머지 4명은 원래부터 명단에 없던 학생이라 예정대로 전시 전용 프로필로 작품만 보존.

**결정 번복 — 자기 작품 반응을 허용하기로:**
2단계에서 "자기 작품에 반응 금지"로 설계했으나, 실제 데이터에서 **전체 반응 93건 중 15건(16%)이 자기 작품 반응**이었습니다. 초등학생이 방금 만든 자기 엽서에 하트를 누르는 것은 자연스러운 사용이고, 막으면 그 기록이 사라집니다. 순위 정렬을 이미 없앴으므로 "자기 점수 올리기" 문제도 크지 않습니다.
**되돌릴 수 있는 쪽을 택했습니다** — 지금 허용해두면 나중에 규칙 한 줄로 막을 수 있지만, 지금 버린 기록은 복구할 수 없습니다. (반응 60건 → 75건)

**전환 준비 중 발견해 고친 보안 구멍:**
`users` 컬렉션을 "로그인한 사용자 누구나" 읽을 수 있게 해뒀는데, 그러면 **학생도 전교생 명단과 학급별 참여 통계를 읽을 수 있었습니다.** 기존 백엔드는 이 조회에 관리자 토큰을 요구했으므로 그 수준으로 좁혔습니다(`본인 또는 관리자`). 갤러리는 제출물에 복사해둔 이름을 쓰므로 영향 없음. 검증: 학생의 전체 명단 조회·타인 프로필 조회 차단, 본인 프로필 조회 허용, 비로그인 갤러리 조회 허용.

**시험 스크립트 3종을 정책 변경에 맞춰 갱신:** 자기 작품 반응 시험(거부→허용), 검증 스크립트의 자기 반응 표시(실패→정상), 사용자 수 검증(전체 조회 불가가 정상이므로 적재 성공 건수로 대체).

**실제 화면 확인(firebase 모드):** 전자화랑 24건 렌더링·이미지 실패 0 ✅ · 반응 조회 ✅ · 로그인 ✅ · 젭퀴즈 통계(로그인 후) 3회차 완료 242명 ✅

**남은 전환 절차:** `BACKEND_MODE` 기본값을 `firebase` 로 바꾸고 배포 → 접수 재개. 되돌리려면 기본값을 `sheets` 로 되돌리면 되고, 원본 스프레드시트는 그대로 남아 있습니다.

### 6단계: 실제 전환 (2026-08-26)

`BACKEND_MODE` 기본값을 `sheets` → **`firebase`** 로 변경. 이제 모든 학생이 Firestore 를 씁니다.

**전환 직전에 처리한 위험 — 백엔드 선택이 브라우저에 고착되는 문제:**
기존 스위치는 선택값을 `localStorage` 에 영구 저장했습니다. 그대로 전환하면 시험 중 `sheets` 로 고정해둔 브라우저는 **전환 후에도 계속 옛 백엔드에 기록**하게 되어 데이터가 두 곳으로 갈라집니다.
→ 페이지 로드 시 `localStorage` 의 옛 값을 **제거**하고, `?backend=` 로 바꾼 값은 `sessionStorage`(탭 닫으면 소멸)에만 두도록 변경. 그래서 누구도 잘못된 백엔드에 영구히 묶이지 않습니다.
→ `index.html` 의 모듈 로더와 `app.js` 의 `BACKEND_MODE` 가 **같은 기준으로 판단**하도록 맞췄습니다(둘이 어긋나면 모듈 없이 firebase 모드로 들어가는 사고가 납니다).

**검증:** `localStorage` 에 `sheets` 를 심어둔 상태로 접속 → 옛 값 제거되고 firebase 로 전환·모듈 로드·정상 응답 ✅ / `?backend=sheets` 로 되돌리기 → 옛 백엔드 정상 동작, 세션에만 저장되고 영구 저장 안 됨 ✅

**되돌리는 방법:** `BACKEND_MODE` 기본값을 `sheets` 로 되돌려 배포하면 즉시 복귀합니다. **원본 스프레드시트와 Apps Script 백엔드는 그대로 살아 있습니다.**

### 비밀번호 찾기 기능 구현 (2026-08-26)

**설계 과정에서 확인한 제약:**
- Firebase Auth 는 비밀번호 변경에 **본인 세션이나 관리자 권한**을 요구합니다. 비밀번호를 잊은 학생에겐 둘 다 없습니다.
- **Firebase 콘솔도 관리자가 비밀번호를 직접 지정하는 기능을 제공하지 않습니다** (재설정 메일 발송만 가능). 검색으로 확인.
- **Cloud Functions 는 Blaze(결제 계정) 필요** — 실제로 배포를 시도해 `must be on the Blaze plan` 오류로 확인.
- 계정 이메일이 `@soro.local`(실존하지 않는 주소)이라 Firebase 기본 재설정 메일도 도달하지 않습니다. 선생님 지메일 `+주소`로 480개를 재등록하는 방안을 제시했으나 사용자가 부담이 크다고 판단해 제외.

**거부한 방안 — 학년/반/번호/이름만으로 재설정:**
그렇게 하면 **같은 반 친구 누구나 남의 비밀번호를 바꿀 수 있습니다.** 그 정보는 갤러리에도 노출되어 있고, 이는 1번 항목에서 막았던 취약점을 그대로 되살리는 것이라 구현하지 않았습니다.

**최종 방식 — 학생이 요청하고, 학생이 직접 정합니다:**
```
학생: "비밀번호 초기화" → 요청 전송 (Firestore, 비로그인 허용)
선생님: 관리자 화면에서 승인 → 임시 비밀번호 a1234567! 로 초기화  ← 여기만 서버
선생님이 임시 비밀번호를 학생에게 알려줌                        ← 본인 확인 역할
학생: 임시 비밀번호로 로그인 → 새 비밀번호 강제 설정 화면
학생이 직접 입력한 비밀번호 적용                                ← 서버 불필요
```

마지막 단계는 학생이 **로그인된 상태**라 `updatePassword()` 로 브라우저에서 바로 처리됩니다. 서버가 필요한 건 "임시 비밀번호로 바꾸기" 한 곳뿐입니다.

**Apps Script 를 그 한 곳에만 사용:** 이미 배포되어 있어 새 인프라가 필요 없고 무료입니다. 속도 우려는 이 경우 해당되지 않습니다 — 초기화는 주당 몇 건 수준이고, 학생들의 일상 동작(로그인·갤러리·제출)은 전부 Firestore 그대로입니다.

**구현 내용:**
- Apps Script `resetStudentPassword` — **Firebase ID 토큰으로 관리자인지 검증**한 뒤 Identity Toolkit 으로 비밀번호 변경. 관리자 자신은 이 경로로 초기화 불가(자기 잠금 방지). `migration/appsscript.json` 에 필요한 OAuth 스코프 명시.
- Firestore `passwordResets/{uid}` — 문서 ID 를 uid 로 고정해 학생당 대기 요청 1개만. **비로그인 생성 허용**(로그인을 못 하는 상태이므로), 존재하는 계정에만, `pending` 상태로만. 수정 불가, 조회·삭제는 관리자만.
- `firebase-backend.js` — `resetPassword`(요청) · `getPasswordResets` · `approvePasswordReset`(승인+`mustChangePassword` 표시) · `changeOwnPassword` · `needsPasswordChange`
- 학생 화면 — 로그인 직후 `mustChangePassword` 확인 후 새 비밀번호 설정 모달 강제 표시
- 관리자 화면 — 대기 요청 목록과 승인 버튼, 승인 시 임시 비밀번호를 알림창으로 표시

**규칙 검증 (3/3):** 비로그인 요청 생성 허용 ✅ · 없는 계정으로는 요청 불가 ✅ · 비로그인으로 요청 목록 훔쳐보기 차단 ✅

**⚠️ 아직 검증 못 한 것:** Apps Script 가 실제로 Firebase 비밀번호를 바꿀 수 있는지는 **재배포와 권한 승인 후에야 확인 가능**합니다. `ScriptApp.getOAuthToken()` 으로 프로젝트 소유자 권한을 써서 Identity Toolkit 을 호출하는 방식인데, 실패하면 오류 메시지에 권한 승인 확인 안내가 나오도록 해뒀습니다.

### 권한 확인 과정에서 드러난 문제 두 가지 (수정 완료)

**1. 확인용 함수가 404 를 반환** — 제가 `/v1/projects/{id}/config` 를 호출했는데 `/config` 는 **v2 전용 경로**입니다. 실제 초기화 코드의 문제가 아니라 확인용 코드만 틀린 것이었습니다. 실제 초기화가 쓰는 것과 같은 계열(`/v1/projects/{id}/accounts:lookup`)을 읽기 전용으로 호출하도록 교체했습니다. 존재하지 않는 계정을 조회하므로 아무것도 바꾸지 않습니다.

**2. 토큰 검증 방식이 잘못됨** — `verifyFirebaseAdmin` 이 `accounts:lookup` 에 OAuth Bearer 토큰을 쓰고 있었으나, ID 토큰 검증은 **웹 API 키(`?key=`)** 를 쓰는 것이 정석입니다. 웹 API 키는 원래 브라우저에 공개되는 값이라 비밀이 아닙니다.
→ **이 방식은 직접 확인했습니다.** 학생 토큰으로 uid(`8ffa0f61740cc152c7ba289e8985`)를 정확히 조회했고, 관리자 uid 와 비교하는 구조가 정상 동작합니다.

### Apps Script 의 GCP 프로젝트를 Firebase 프로젝트로 변경

권한 확인 실행 시 403 이 났고, 오류에 적힌 프로젝트 번호가 **`144157837805`** 였습니다. 이는 Firebase 프로젝트(`961856422255`)가 아니라 **Apps Script 가 뒤에서 자동 생성한 숨겨진 GCP 프로젝트**입니다. Apps Script 가 구글 API 를 호출하면 구글은 **호출하는 쪽 프로젝트**에 그 API 가 켜져 있는지 먼저 확인하는데, 이 자동 생성 프로젝트는 사용자에게 관리 권한이 없어(콘솔에서 "추가 액세스 권한 필요" + `resourcemanager.projects.get` 누락) **API 를 켜는 것이 불가능**했습니다.

→ 해결 방향(진행 중): Apps Script 의 GCP 프로젝트를 Firebase 프로젝트(`961856422255`)로 변경. Identity Toolkit API 는 Firebase 인증이 쓰는 API 라 이미 켜져 있습니다. 사전 작업으로 해당 프로젝트에 OAuth 동의 화면을 구성했습니다.

**주의:** 이 Apps Script 는 독립 프로젝트가 아니라 **기존 구글 시트에 붙어 있는 스크립트**입니다(시트 → 확장 프로그램 → Apps Script). 프로젝트 설정(⚙️)에서 GCP 프로젝트 변경은 그대로 가능합니다.

**결과: 통과.** GCP 프로젝트 변경 후 `checkFirebaseAccess` 가 성공했습니다.

```
✅ 준비 완료 — Firebase 프로젝트 'soro-migration-test' 에 접근할 수 있습니다.
```

이로써 **비밀번호 초기화 기능에서 마지막까지 검증되지 않았던 부분이 해소되었습니다.**

### 배포된 엔드포인트 실측 검증 (4/4)

실제 배포 주소에 직접 요청을 넣어 확인했습니다.

| 확인 | 결과 |
|---|---|
| 배포가 살아있는가 (`getContestLocks`) | ✅ 정상 응답 |
| 토큰 없이 초기화 시도 | ✅ `관리자 권한이 필요합니다.` |
| 가짜 토큰으로 초기화 시도 | ✅ `관리자 권한이 필요합니다.` |
| **유효한 학생 토큰**으로 초기화 시도 | ✅ `관리자 권한이 필요합니다.` |

마지막 항목이 핵심입니다. 위조 토큰이 아니라 **실제로 발급받은 정상 학생 토큰**인데도 거부되었습니다 — uid 가 관리자와 다르기 때문입니다. 학생이 남의 비밀번호를 초기화할 수 없다는 것이 실증되었습니다.

**검증 과정의 함정 하나 기록:** `curl -L -X POST` 로 부르면 405 와 "페이지를 찾을 수 없음" HTML 이 돌아옵니다. 앱스크립트는 302 로 결과 주소를 알려주는 구조인데, `-X POST` 가 리다이렉트 뒤에도 POST 를 강제해서 결과 주소(GET 전용)를 잘못 부르기 때문입니다. `-X POST` 를 빼고 `-d` 만 쓰면 정상입니다. 배포가 죽은 것처럼 보이지만 아닙니다.

### 남은 것: 공모전이 전부 잠겨 있음

Firestore 이전 당시 데이터를 얼리려고 전부 잠갔고, 아직 그대로입니다.

```
🔒 cuttoon  🔒 keyring  🔒 library  🔒 pixelart  🔒 sound_album  🔒 transcription
zepquiz_active_round = 3
```

관리자 화면에서 필요한 공모전을 다시 열어야 학생들이 제출할 수 있습니다. **9월 온라인 도서관(`library`)은 일정이 2026.8.24 ~ 9.30 이므로 지금 열려 있어야 하는데 잠겨 있습니다.**

## 젭퀴즈 잠금 기능 (요청: "젭퀴즈를 잠궈줘")

### 왜 코드를 고쳐야 했나

젭퀴즈에는 다른 공모전 같은 잠금 설정이 없습니다. `contest_lock_*` 목록에서 의도적으로 제외되어 있고(`lockableContests`), 오직 `zepquiz_active_round` 하나로만 굴러갑니다. 관리자 화면의 회차 선택 목록에도 1~6회차뿐이라 **"전부 닫기"를 고를 방법이 아예 없었습니다.** 그래서 설정값만 바꾸는 게 아니라 기능을 만들었습니다.

**값 설계:** 존재하지 않는 회차인 `0` 을 "전체 잠금"으로 씁니다. 어떤 회차도 `zepquiz_0` 이 아니므로 학생 화면에서 카드가 사라지고, 보안 규칙의 `isActiveZepRound` 도 `zepquiz_0` 을 찾으므로 서버에서도 제출이 전부 막힙니다. 규칙 수정이 필요 없습니다.

### 이 과정에서 발견한 버그 두 가지 (둘 다 운영 중이었음)

**1. `0` 을 저장해도 클라이언트가 조용히 `3` 으로 되돌림** — `app.js` 의 활성 회차 해석이 `parsedRound < 1 ? 3` 이었습니다. 잠가도 3회차가 다시 열렸을 것입니다. `< 0` 으로 고쳐 `0` 을 정상 값으로 살렸습니다. 빈 값·문자열·음수는 여전히 `3` 으로 떨어집니다.

**2. [심각] 공모전 잠금 정보가 운영 사이트에서 아예 로드되지 않고 있었음**

`callBackend` 은 Firebase 모듈이 아직 안 붙었으면 기다리지 않고 즉시 실패합니다. 그런데 모듈은 CDN 에서 SDK 를 한 번 더 받아오므로 `app.js` 보다 늦게 끝납니다. 페이지가 열리자마자 부르는 `fetchContestLocks()` 가 항상 이 창에 걸렸고, **재시도가 없어 그대로 끝났습니다.**

운영 사이트(`26soro.vercel.app`)에서 직접 확인한 실제 상태:
```
contestLocks = {}          ← 서버 설정을 하나도 못 받아옴
currentActiveZepRound = 3  ← 기본값
zepquiz_3 → "접수 중"       ← 서버 상태가 아니라 기본값이 그대로 보이는 것
```
즉 **관리자가 설정한 잠금이 학생 화면에 반영되지 않고 있었습니다.** 다른 공모전은 "정보 없으면 마감"이라 안전한 쪽으로 실패했지만, 젭퀴즈만 기본값 때문에 열린 쪽으로 실패했습니다.

→ `index.html` 이 모듈 로딩 완료를 알리는 약속(Promise)을 미리 만들고, `firebase-backend.js` 가 준비되면 풀어주고, `callBackend` 이 최대 10초까지 기다리도록 했습니다. 모듈 로딩 자체가 실패해도 `onerror` 로 풀어줘 멈추지 않습니다.

→ 추가로 젭퀴즈도 "확인 전에는 마감"(`zepRoundLoaded`)으로 바꿔 다른 공모전과 원칙을 맞췄습니다.

### 변경 내용

- `index.html` — 관리자 화면에 **🔒 젭퀴즈 잠금 / 🔓 잠금 해제** 버튼 추가. 회차 드롭다운은 통계 조회용이라 건드리지 않았습니다. 모듈 준비 약속 추가. 캐시 갱신을 위해 `app.js?v=2.7.0`, `firebase-backend.js?v=3`
- `app.js` — `ZEPQUIZ_LOCKED`/`zepRoundLoaded`, 되돌림 버그 수정, 상태 판정에 잠금 반영, 회차 설정과 잠금을 `applyActiveZepRound` 로 통합, `updateZepActiveLabel` 로 라벨·버튼 상태 동기화
- `firebase-backend.js` — 준비 완료 신호

### 검증 (로컬 브라우저 실측)

| 확인 | 결과 |
|---|---|
| 잠금 정보가 실제로 로드되는가 | ✅ `{}` → 6개 공모전 값 정상 수신 |
| "준비되지 않았습니다" 오류 | ✅ 사라짐 |
| 서버값 `0` 일 때 학생 화면 | ✅ 젭퀴즈 카드 사라짐, 나머지 6개는 그대로 |
| 서버값 `0` 일 때 1~6회차 상태 | ✅ 전부 "마감" |
| 관리자 라벨 | ✅ "3회차" → "잠김 (접수 없음)" (색상도 회색으로) |
| 버튼 문구 | ✅ "🔒 젭퀴즈 잠금" → "🔓 젭퀴즈 잠금 해제" |
| 잘못된 값 방어 | ✅ 빈 값·`abc`·`-1` 은 여전히 3회차로 |

### 남은 절차

제가 설정값을 직접 바꾸려면 저장된 로그인 자격증명을 읽어야 해서 차단되었고, 우회하지 않았습니다. 배포 후 **관리자 화면에서 🔒 젭퀴즈 잠금 버튼을 한 번 누르시면** 됩니다. 그 전까지 젭퀴즈는 열린 상태입니다.

### 재점검에서 추가로 고친 것

**1. 잠긴 상태로 페이지를 열면 잠금 해제 시 엉뚱한 회차가 열림** — 잠금에는 대응하는 회차가 없어 회차 목록 동기화를 건너뛰는데, 그러면 목록이 첫 항목(1회차)에 머뭅니다. 이 상태에서 "잠금 해제"를 누르면 3회차가 아니라 1회차가 열립니다. 확인창에 어느 회차가 열리는지와 바꾸는 방법을 명시했습니다.

**2. 대기 타이머가 호출마다 쌓임** — `Promise.race` 의 10초 타이머를 정리하지 않아 호출마다 하나씩 남았습니다. `clearTimeout` 추가.

### 재점검 결과 (이상 없음 확인)

| 점검 항목 | 결과 |
|---|---|
| `getContestStatus` 앞의 `return "active"` 가 잠금을 무력화하는가 | 관리자 전용 분기 — 학생 영향 없음 |
| 카드를 숨겨도 다른 경로로 젭퀴즈를 열 수 있는가 | 진입점은 카드 클릭 하나뿐이고, `openContestDetails` 도 `closed` 면 차단 |
| 앱스크립트(sheets 모드)가 제출을 막는가 | 활성 회차 0 이면 1~6회차 전부 "현재 진행 중인 회차가 아닙니다" |
| Firestore 규칙이 막는가 | `isActiveZepRound` 가 `zepquiz_0` 을 찾으므로 전부 거부 |
| 서버 응답 전 상태 | 젭퀴즈 카드 없음, 전 회차 `closed`, 상세 열기 차단 |
| 잘못된 활성 회차 값 | 빈 값·`abc`·`-1` 은 3회차로 폴백, `0` 만 잠금으로 해석 |
| 다른 공모전에 영향 | 없음 — 도서관 등 6개는 그대로 |

**참고:** 로컬 확인 중 `Firestore/Listen/channel` 400 이 보이는데, SDK 실시간 채널의 핸드셰이크 실패로 자동 폴백되는 것이며 운영 사이트에서는 나오지 않습니다. 데이터 수신에 문제 없고 이번 변경과 무관합니다.

## main 통합 및 배포 (2026-08-26)

`modify` 브랜치의 5개 커밋을 `main` 에 통합하고 GitHub 에 푸시했습니다. Vercel 이 `main` 을 자동 배포하므로 이 시점부터 운영에 반영됩니다.

| 커밋 | 내용 |
|---|---|
| `0a7b7e6` | 학생이 직접 새 비밀번호를 정하는 비밀번호 초기화 |
| `015cf57` | Apps Script 의 Identity Toolkit 호출 주소·인증 방식 수정 |
| `e68992c` | 비밀번호 초기화 경로 실측 검증 결과 기록 |
| `836b8eb` | 젭퀴즈 잠금 기능 신설 + 공모전 잠금 로딩 문제 수정 |
| `1edeed5` | 잠금 해제 시 열릴 회차 명시, 대기 타이머 정리 |

**배포에 포함된 것 중 운영에 즉시 영향이 있는 변경:**

1. **공모전 잠금이 이제 실제로 반영됩니다.** 그동안 잠금 정보가 로드되지 않아 관리자 설정이 학생 화면에 전달되지 않았습니다. 배포 직후부터는 Firestore 에 저장된 값이 그대로 화면에 나타납니다. 현재 6개 공모전이 전부 잠겨 있으므로, **열려면 관리자 화면에서 직접 열어야 합니다.**
2. **캐시 갱신** — `app.js?v=2.7.0`, `firebase-backend.js?v=3`. 학생 기기가 옛 파일을 계속 쓰지 않도록 버전을 올렸습니다.

### 배포 후 할 일 (사람이 직접)

- [ ] 관리자 화면에서 **🔒 젭퀴즈 잠금** 클릭 — 자격증명 문제로 제가 대신 누를 수 없습니다
- [ ] 9월 온라인 도서관(`library`) 열기 — 일정이 2026.8.24 ~ 9.30 인데 현재 잠겨 있습니다
- [ ] 그 외 운영할 공모전 선택적으로 열기
- [ ] 비밀번호 초기화 실제 흐름 한 번 확인 (학생 요청 → 승인 → 임시 비밀번호 로그인 → 새 비밀번호 설정)

### 배포 후 운영 사이트 실측 확인

`26soro.vercel.app` 에서 직접 확인했습니다.

| 항목 | 배포 전 | 배포 후 |
|---|---|---|
| `app.js` 버전 | 2.6.1 | **2.7.0** ✅ |
| `contestLocks` | `{}` (로드 실패) | **6개 공모전 값 정상 수신** ✅ |
| `zepRoundLoaded` | 없음 | `true` ✅ |
| 🔒 젭퀴즈 잠금 버튼 | 없음 | **존재** ✅ |
| 카드 상태 표시 | 전부 기본값 | 서버 설정 그대로 반영 ✅ |

배포 직후 학생 화면에 실제로 보이는 상태:
```
keyring       → 접수 마감      cuttoon    → 접수 마감
library       → 접수 마감      pixelart   → 접수 마감
transcription → 접수 마감      sound_album → 접수 마감
zepquiz_3     → 접수 중        ← 아직 잠그지 않았습니다
```

이제 관리자 화면의 설정이 학생 화면에 실제로 전달됩니다. 젭퀴즈는 설정값이 아직 3이라 열려 있으며, **버튼을 눌러야 잠깁니다.**

## 관리자 권한이 꺼져 있던 문제

사용자가 "5학년 1반 27번 김태호로 접속하면 관리자 모드가 되어야 하는데?" 라고 물어 확인한 결과, **관리자 권한이 부여되어 있지 않았습니다.**

근거 세 가지가 일치했습니다.
- 마이그레이션 산출물(`firestore-users.json`) 484명 중 `admin` 필드를 가진 문서 **0개**
- 이 작업일지에 "시험용으로 임시 부여했던 권한을 **회수**한 뒤 거부되는 것까지 확인" — 회수 후 복구하지 않음
- `admin: true` 를 설정하는 코드가 저장소 어디에도 없음 (당시 수동으로 넣었던 값)

`firebase-backend.js` 의 `login` 은 `users/{uid}.admin === true` 일 때만 `adminToken` 을 발급하므로, 이 상태로는 로그인해도 학생 화면만 보입니다. 그 결과 **젭퀴즈 잠금도 도서관 개방도 할 수 없는 상태**였습니다.

### 앱 안에서는 고칠 수 없는 이유

```
allow update: if (isOwner(uid) && !('admin' in request.resource.data)) || isAdmin();
```
본인이 자기 문서에 `admin` 을 붙이는 것을 규칙이 막습니다. 학생이 스스로 관리자가 되는 것을 막으려고 만든 장치인데, 선생님에게도 똑같이 적용됩니다. **관리자가 되려면 이미 관리자여야 하는 순환**입니다.

### 해결: 앱스크립트로 부여 (`grantAdminToTeacher`)

프로젝트 소유자 권한(`ScriptApp.getOAuthToken()`)으로 부르는 Firestore REST 는 보안 규칙을 거치지 않으므로 이 순환을 피할 수 있습니다. 앞서 `checkFirebaseAccess` 로 이 경로가 살아있음을 이미 확인했습니다.

- `doPost` 에 연결하지 않았습니다 — 웹으로는 호출 불가, 편집기에서 소유자만 실행 가능
- `updateMask.fieldPaths=admin` 을 반드시 붙였습니다. 없으면 PATCH 가 문서를 통째로 덮어써 학년·반·번호·이름이 날아갑니다
- 실행 후 결과 문서를 되읽어 이름·학년·반·번호와 `admin` 값을 로그에 찍습니다 (엉뚱한 문서를 건드리지 않았는지 확인용)
- 반영하려면 **사이트에서 로그아웃 후 재로그인**해야 합니다. 관리자 토큰은 로그인 시점에 발급됩니다

## 운영 반영 확인 (2026-08-27)

선생님이 Firebase 콘솔에서 `users/9009084b5e62077445ee99bc1169` 에 `admin: true` (boolean) 를 넣고 재로그인한 뒤, 관리자 화면에서 직접 두 가지를 실행했습니다.

Firestore 실제 값 (수정 시각 포함):
```
zepquiz_active_round   = 0      🔒 잠김   (2026-08-27 04:08:17 UTC)
contest_lock_library   = false  🔓 열림   (2026-08-27 04:08:45 UTC)
```

**이 두 값이 바뀌었다는 것 자체가 관리자 권한이 정상 작동한다는 증거입니다.** `settings` 쓰기는 규칙상 `users/{uid}.admin == true` 인 계정만 가능하므로, 값이 바뀌었다면 관리자 인증이 통과한 것입니다. (관리자 비밀번호가 없어 로그인으로는 확인할 수 없었던 부분이 이렇게 간접 확인되었습니다.)

운영 사이트(`26soro.vercel.app`) 학생 화면 실측:

| 공모전 | 표시 |
|---|---|
| 온라인 도서관 | **접수 중** 🔓 |
| 키링 / 컷툰 / 디지털 필사 / 픽셀아트 / 사운드 앨범 | 접수 마감 |
| 젭퀴즈 | **카드 자체가 없음** (6개 회차 전부 사라짐) |

- 헤더 요약: "진행 중인 대회 **1개**"
- `getContestStatus("library")` → `active` (상세 열기 허용)
- `getContestStatus("zepquiz_3")` → `closed` (상세 열기 차단)

### 이번 작업 마무리

- ✅ 비밀번호 초기화 — Apps Script 권한 확보, 엔드포인트 거부 경로 4/4 실측 검증
- ✅ 젭퀴즈 잠금 기능 신설 — 관리자 화면 버튼으로 잠금/해제
- ✅ 공모전 잠금이 학생 화면에 반영되지 않던 문제 수정 (운영 중이던 버그)
- ✅ 관리자 권한 복구
- ✅ 젭퀴즈 잠금, 온라인 도서관 개방 완료

**남은 확인 사항:** 비밀번호 초기화 전체 흐름을 실제 학생 계정으로 한 번 통과시켜 보는 것 (요청 → 승인 → 임시 비밀번호 로그인 → 새 비밀번호 설정). 각 구간은 검증했지만 처음부터 끝까지 이어서 해본 적은 없습니다.

## 비밀번호 초기화 전체 흐름 시험 — 버그 2건 발견 및 수정 (2026-08-27)

구간별로는 검증했지만 처음부터 끝까지 이어서 해본 적이 없어 실제로 돌려봤습니다. **두 건 다 실사용을 막는 버그였고, 흐름을 돌려보지 않았으면 발견하지 못했을 것들입니다.**

시험 방법: 운영 사이트에 배포된 코드로 시험용 계정(`6_9_99_초기화테스트`)을 만들고, 실제 학생과 똑같이 로그아웃 상태에서 요청을 넣어 봤습니다.

### 버그 1 — 로그아웃 상태에서는 요청 자체가 불가능했음

`resetPassword` 가 계정 존재를 확인하려고 `users/{uid}` 를 읽는데, 그 문서는 **본인이나 관리자만** 읽을 수 있습니다. 비밀번호를 잊은 학생은 로그인을 못 하므로 **반드시 `permission-denied`** 가 납니다.

```
요청 시점 로그인 여부: false
결과: "네트워크 연결이 지연되고 있습니다. 잠시 후 다시 시도해 주세요."
콘솔: FirebaseError: Missing or insufficient permissions. (code: permission-denied)
```

오류 문구도 "네트워크 지연"이라 원인이 전혀 드러나지 않았습니다. 이 기능을 쓸 수 있는 유일한 대상이 **정확히 이 기능을 쓸 수 없는** 상태였습니다.

→ 존재 확인을 없앴습니다. 보안 규칙이 이미 `exists()` 로 검사하므로 클라이언트 확인은 불필요했고, 애초에 불가능한 확인이었습니다. `permission-denied` 를 잡아 "가입된 계정이 없습니다" 로 안내합니다. 화면 표시용 학년·반·번호·이름은 프로필 대신 학생이 방금 입력한 `userKey` 에서 뽑습니다.

### 버그 2 — 이전 이후에 가입한 학생은 영영 초기화 불가

```
uidOf("6_9_99_초기화테스트") = 99b841ee4360f5e72d1b6ed5a677   ← 초기화가 찾는 값
실제 계정의 uid              = ypG404yy3DMVMpawqT3vBikHZAH2   ← Firebase 가 발급한 값
```

이전해 온 484명은 uid 를 직접 지정해 넣어서 `uidOf(userKey)` 와 일치하지만, **그 뒤로 가입하는 학생은 Firebase 가 무작위 uid 를 발급**합니다. 비밀번호를 잊은 학생은 로그인을 못 해 자기 uid 를 알 수 없고 `users` 도 읽을 수 없으니, 요청을 만들 방법이 없습니다.

→ `accountIndex/{uidOf(userKey)}` → `{ uid: 실제uid }` 색인을 두었습니다.
- 가입 시 기록합니다.
- **로그인 시에도 보정합니다** — 이 수정 이전에 가입한 계정도 한 번 로그인하면 자동으로 채워집니다. 이전해 온 계정은 해시 비교만 하고 끝나 추가 통신이 없습니다.
- 승인 시 색인으로 실제 uid 를 찾습니다. 색인이 없으면 이전 계정이므로 문서 id 가 곧 실제 uid 입니다.
- 규칙: `exists(users/$(uid)) || exists(accountIndex/$(uid))`. 규칙 안의 `exists()` 는 읽기 권한과 무관하게 동작하므로 로그아웃 상태에서도 검사가 됩니다. 색인 읽기는 관리자에게만 열었고, 담긴 값은 uid 하나뿐입니다.

### 함께 고친 것 — 잘못 들어온 요청을 지울 수 없었음

관리자 화면에 **승인 버튼만** 있었습니다. 오타로 들어온 요청을 없애려면 승인해야 했고, 그러면 멀쩡한 학생의 비밀번호가 초기화됩니다. `resolvePasswordReset` 은 이미 구현돼 있는데 버튼이 없었습니다. **무시** 버튼을 추가했습니다 (요청만 지우고 비밀번호는 건드리지 않습니다).

### 검증 결과

| 시험 | 결과 |
|---|---|
| 이전해 온 계정(484명) — 로그아웃 상태 요청 | ✅ 성공 |
| 이전 이후 가입한 계정 — 로그아웃 상태 요청 | ✅ 성공 (수정 전 실패) |
| 없는 계정 요청 | ✅ "가입된 계정이 없습니다" 로 거부 |
| 기존 계정 색인 자동 보정 | ✅ 로그인 한 번으로 채워짐 |

**아직 남은 구간:** 선생님이 승인 → 임시 비밀번호로 로그인 → 새 비밀번호 설정. 승인은 관리자 권한이 필요해 제가 할 수 없습니다.

시험 편의를 위해 대기 중인 요청 두 건을 만들어 두었습니다.
- `6학년 9반 99번 초기화테스트` — 승인해 주시면 나머지 구간을 제가 이어서 검증합니다
- `5학년 1반 27번 김태호` — 이전 계정 경로 검증용. **승인하지 마시고 [무시] 로 지워 주세요.** (앱스크립트가 관리자 자기 초기화를 거부하므로 승인해도 비밀번호는 바뀌지 않습니다. 무시 버튼 시험도 겸합니다)

### 버그 3 — 두 번 요청하면 "가입된 계정이 없습니다"

요청은 `setDoc` 으로 저장하는데, 이미 대기 중인 요청이 있으면 이것이 생성이 아니라 **수정**이 됩니다. 규칙이 수정을 전부 막고 있어서 거부되고, 화면에는 계정이 없다는 안내가 떴습니다.

눌러도 아무 반응이 없어 한 번 더 누른 아이에게 **"네 정보가 틀렸다"** 고 말하는 셈이라, 실제로는 제대로 입력했는데 자기 잘못인 줄 알게 됩니다.

→ 수정을 생성과 **똑같은 조건**으로 허용했습니다. 조건이 같으므로 남의 요청을 엉뚱한 내용으로 바꿔 놓을 수는 없습니다.

```
② 첫 요청 (생성)      → 성공 ✅
③ 두 번째 요청 (수정)  → 수정 전 거부 → 수정 후 성공
```

### 승인이 실패했던 이유 — 관리자 화면이 옛 버전

선생님이 99번을 승인했는데도 비밀번호가 바뀌지 않았습니다. 확인 결과:

```
① 옛 비밀번호로 로그인    → 성공   ← 바뀌지 않음
② 임시 비밀번호로 로그인  → 실패
③ 비밀번호 변경 요구      → 없음
```

`accountIndex` 는 정상적으로 존재합니다(요청이 통과하는 것으로 확인). 원인은 **브라우저가 옛 `app.js` 를 쓰고 있었던 것**입니다. 옛 코드에는 실제 uid 를 찾는 단계가 없어 존재하지 않는 uid 로 초기화를 시도합니다. [무시] 버튼이 보이지 않는다는 것이 옛 버전이라는 증거였습니다.

→ 강력 새로고침(Cmd+Shift+R) 후 다시 승인하면 됩니다.

**교훈:** 관리자 화면을 열어둔 채로 배포하면 옛 코드가 계속 돕니다. 파일 버전(`?v=`)은 올렸지만 `index.html` 자체가 캐시되면 새 버전을 읽지 않습니다.

### 비밀번호 초기화 — 전 구간 통과 (2026-08-27)

선생님이 시험 계정 두 개를 승인한 뒤 나머지를 확인했습니다. **처음부터 끝까지 이어서 성공한 것은 이번이 처음입니다.**

| 확인 | 99번 (로그인 시 색인 보정) | 97번 (가입 시 색인 기록) |
|---|---|---|
| 옛 비밀번호 차단 | ✅ | ✅ |
| 임시 비밀번호 `a1234567!` 로 로그인 | ✅ | ✅ |
| 새 비밀번호 설정 요구 표시 | ✅ | ✅ |

이어서 학생이 직접 비밀번호를 정하는 구간:

| 확인 | 결과 |
|---|---|
| 임시 비밀번호를 그대로 재사용 | ✅ "임시 비밀번호와 다른 비밀번호로 정해 주세요." |
| 6자 미만 | ✅ "비밀번호는 6자 이상으로 정해 주세요." |
| 새 비밀번호 설정 | ✅ 성공 |
| 설정 직후 변경 요구 해제 | ✅ |
| 새 비밀번호로 재로그인 | ✅ |
| 재로그인 후 변경 요구 | ✅ 뜨지 않음 |
| 임시 비밀번호 재사용 시도 | ✅ 막힘 |

두 계정은 색인이 기록된 경로가 서로 다릅니다(가입 시 / 로그인 시 보정). **양쪽 다 통과했습니다.**

### 같은 뿌리의 버그 2건 추가 발견

`uidOf(userKey)` 를 실제 uid 처럼 쓰는 곳이 초기화 말고도 있었습니다. 전부 **이전 이후에 가입한 학생에게만** 나타나는 문제입니다.

**`deleteUser`** — 계산한 uid 로 문서를 지우려 해서, 신규 가입 학생을 삭제하면 **아무것도 지워지지 않은 채 "삭제했습니다" 만 떴습니다.** 제출물도 프로필도 그대로 남습니다. 색인으로 실제 uid 를 찾도록 고쳤고, 색인 자체도 함께 지웁니다.

**`getReactions`** — 반응은 실제 uid 로 저장되는데 조회는 계산한 uid 로 비교했습니다. 그래서 신규 가입 학생은 **자기가 누른 하트가 눌리지 않은 것처럼 보였습니다.** 로그인 상태에서는 실제 uid 가 확실하므로 그것을 쓰도록 고쳤습니다.

공용 헬퍼 `realUidFor()` 로 정리했습니다.

**남은 정리:** 시험용 계정 `6학년 9반 99번 초기화테스트`, `6학년 9반 97번 초기화테스트2` 삭제. 관리자 화면의 학생 삭제로 지우면 방금 고친 `deleteUser` 검증도 겸합니다. (로그인 계정 자체는 Firebase 콘솔에서 지워야 합니다 — Auth 계정 삭제는 관리자 SDK 가 필요합니다.)

**반응 수정 검증 (운영 사이트):** 이전 이후 가입한 계정(`uid: ypG404yy…`)으로 로그인해 작품 하나에 하트를 눌렀다가 되돌렸습니다.

```
누른 뒤 '내 반응'으로 잡히는가 → ✅ 잡힘   (수정 전에는 안 잡혔습니다)
되돌린 뒤                      → ✅ 깨끗이 지워짐
```
