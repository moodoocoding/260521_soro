# 작업 내역

이 문서는 `260521_soro` 저장소에서 진행하는 작업을 시간순으로 기록합니다.
코드를 수정할 때마다 이 파일에 항목을 추가합니다.

---

## 2026-08-14

### 저장소 클론
- `https://github.com/moodoocoding/260521_soro.git` 을 `main` 브랜치 기준으로 로컬에 클론.
- 작업용 브랜치 `modify` 생성 및 전환.

### 전체 코드 리뷰 수행 (커밋 없음, 조사만 진행)
`app.js`(~7,900줄), `index.html`, `style.css` 전체를 7가지 관점(정확성/비동기, 관리자 권한 경계, 파일 간 참조, 재사용성, 단순화, 효율성, 설계 깊이)으로 리뷰. 검증을 거쳐 아래 10건을 확정. **아직 아무것도 수정하지 않은 상태.**

| # | 심각도 | 요약 | 위치 | 상태 |
|---|--------|------|------|------|
| 1 | 심각(보안) | 백엔드(Apps Script) API에 서버 측 권한 검사가 전혀 없음 — API URL만 알면 누구나 학생 데이터 조회/삭제, 설정 변경 가능 | app.js:7392 등 (doPost, 7125-7887 임베디드 백엔드 코드) | 코드 수정 완료, **실제 배포 대기 중** (아래 상세 참고) |
| 2 | 심각(보안) | `checkIsAdmin()`이 localStorage 값 비교뿐이라 관리자 권한이 클라이언트에서 위조 가능 | app.js:641 | 코드 수정 완료 (아래 상세 참고) |
| 3 | 정정됨 | 비-젭퀴즈 공모전이 학생에게 항상 "마감"으로 표시됨 → **의도된 동작으로 확인됨** (자동 개폐 로직의 운영상 문제로 수동 통제 중) | app.js:649 | 의도된 동작 (수정 불필요) |
| 4 | 심각(보안) | `submitContest` 백엔드가 회차/잠금 상태/제출자 신원을 검증하지 않아 마감된 공모전 제출이나 가짜 신원 제출이 가능 (단, 실존 학생 사칭까지는 미해결 — 아래 참고) | app.js:7229 | 배포 및 검증 완료 |
| 5 | 중간 | `fetchContestLocks()`의 클램프 로직 때문에 활성 회차를 "1"로 설정해도 항상 "3"으로 되돌아감 | app.js:29 | 수정 완료 |
| 6 | 중간(추정) | 관리자가 젭퀴즈 회차를 빠르게 전환하면 비동기 요청 경쟁으로 오래된 회차 데이터가 화면에 덮어써질 수 있음 | app.js:6560 | 수정 완료 |
| 7 | 중간(추정) | 공모전 상세 드로어를 빠르게 전환하면 다른 공모전의 제출 상태가 잘못 표시될 수 있음 | app.js:2385 | 수정 완료 |
| 8 | 중간(추정) | AI 캘리그라피 생성이 느리게 끝나는 사이 다른 공모전으로 이동해 업로드하면 이전 이미지가 제출될 수 있음 | app.js:3416 | 수정 완료 |
| 9 | 중간 | 관리자 상세평가 버튼(`openAdminEvalModal`)이 정의되지 않은 함수를 호출함 — 클릭해도 아무 동작 안 함 | app.js:6152 | 수정 완료 |
| 10 | 낮음 | 관리자 "표 뷰" 토글이 실제로는 숨겨진 갤러리 컨테이너에 렌더링해서 항상 "준비 중" placeholder만 보임 | app.js:5859 | 수정 완료 |

### 1번 항목 수정: 백엔드에 서버 측 관리자 인증 추가
관련 항목: **#1** (백엔드 권한 검사 부재). 계획 문서: `.claude/plans/parallel-launching-wave.md` (세션 로컬 경로).

**변경 내용 (app.js):**
- 로그인 액션(`login`)에서 인증 성공 시, `requestData.userKey`가 하드코딩된 관리자 식별자(`5_1_27_김태호`)와 일치하면 `Utilities.getUuid()`로 세션 토큰을 발급해 `PropertiesService`에 12시간 만료로 저장하고 응답에 `adminToken`으로 포함.
- `isValidAdminToken(token)` 헬퍼 추가.
- 관리자 전용 액션 9개(`deleteUser`, `getAllSubmissions`(단, `contestId==="all"`인 전체 조회일 때만), `updateSubmissionPrizeStatus`, `updateActiveZepRound`, `updateContestLock`, `updateSubmissionStarStatus`, `getZepQuizStats`, `updateClassPrizeStatus`, `updateClassStudentCount`)에 토큰 검사 추가.
- `deleteSubmission`: 삭제 대상의 실제 소유자(`targetUsername`)와 `requestData.studentUsername`이 일치하거나 유효한 관리자 토큰이 있어야 삭제되도록 변경 (본인 취소는 그대로 허용, 타인이 아이디만 알고 삭제하던 기존 허점도 함께 막힘).
- `resetPassword`: 관리자 계정(`5_1_27_김태호`)에 한해서만 유효한 관리자 토큰을 요구 (조사 중 발견한 연관 취약점 — 안 막으면 비밀번호 재설정으로 토큰 발급 자체를 우회 가능했음).
- 클라이언트: `handleLogin`/`handleSignUp`/`handleResetPassword`가 응답의 `adminToken`을 `currentUser`와 localStorage에 저장. `getAdminToken()` 헬퍼 추가. 위 9개 액션 및 관리자용 `deleteSubmission` 호출부 전부에 `adminToken` 필드 추가. 학생 본인 취소용 `deleteSubmission` 호출부 3곳에는 `studentUsername` 필드 추가.

**계획에서 살짝 벗어난 부분:** `getAllSubmissions`는 원래 계획대로 전부 막으면 로그인 없이도 동작해야 하는 공개 갤러리(키링 공모전, 온라인 도서관)가 깨지는 걸 구현 중 발견해서, `contestId === "all"`(관리자 대시보드의 전체 일괄 조회)일 때만 토큰을 요구하도록 범위를 좁힘. 특정 공모전 지정 조회(공개 갤러리용)는 기존처럼 그대로 둠.

**검증 상태:** 코드 레벨 검토 및 `node --check`로 문법 확인 완료. 실제 동작 확인은 배포 후에만 가능.

**✅ 배포 및 검증 완료 (2026-08-14):** 사용자가 script.google.com에 새 버전으로 배포 완료. 배포 직후 curl로 테스트했을 때 일시적으로 "페이지를 찾을 수 없음" 오류가 떴으나(전파 지연으로 추정), 몇 분 후 재시도하니 정상화됨. 최종 검증 결과:
- `getAllSubmissions`(`contestId:"all"`, 토큰 없음) → 차단됨 ✅
- `getAllSubmissions`(`contestId:"keyring"`, 공개 갤러리) → 정상 작동, 로그인 불필요 유지 ✅
- `getZepQuizStats`(토큰 없음) → 차단됨 ✅
- `deleteUser`(토큰 없음) → 차단됨 ✅
- `getAllSubmissions`(가짜 토큰) → 차단됨 (토큰 값 실제 검증 확인) ✅

남은 것: 실제 관리자 계정으로 로그인해 관리자 대시보드 정상 동작 확인 (Claude가 비밀번호를 모르므로 사용자가 직접 확인 필요).

**🔙 롤백용 백업:** 수정 전 원본 Apps Script 코드를 [doc/backend_backup_2026-08-14_before_admin_auth.gs](doc/backend_backup_2026-08-14_before_admin_auth.gs)에 저장해 뒀습니다. 새 코드를 배포한 뒤 문제가 생기면, script.google.com 편집기 내용을 이 백업 파일 내용으로 다시 덮어쓰고 "배포 관리 → 편집 → 새 버전 → 배포"만 하면 즉시 이전 상태로 되돌릴 수 있습니다. (참고로 git 커밋 이력에도 원본이 그대로 남아있어 이 파일이 없어져도 `git show`로 복구 가능합니다.)

**⚠️ 아직 안 끝남 — 사람이 직접 해야 할 일:**
1. `app.js`의 7125~7887줄(관리자 인증 부분 포함, 새로 삽입된 내용 포함) 전체를 복사해서 **script.google.com의 실제 Apps Script 프로젝트에 붙여넣기**.
2. **배포 → 배포 관리 → 편집(연필) → 새 버전 → 배포** (반드시 기존 배포의 "새 버전"으로 — 그래야 `GOOGLE_SHEET_API_URL`이 안 바뀜).
3. 배포 후 확인: (1) 비관리자 로그인 시 관리자 액션이 거부되는지, (2) 관리자 로그인은 정상 동작하는지, (3) 키링/도서관 공개 갤러리가 여전히 로그인 없이 보이는지, (4) 토큰 없이 `curl`로 `getAllSubmissions`(`contestId:"all"`) 호출 시 거부되는지.

이번 범위에서 제외한 것 (별도 항목으로 남김): `submitContest`의 회차/잠금 검증 부재(#4), 일반 학생 대상 `resetPassword`의 소유권 미검증, 레이스 컨디션들(#6,#7,#8), `openAdminEvalModal` 미정의(#9), 관리자 "표 뷰" 렌더링 오류(#10).

### 2번 항목 수정: `checkIsAdmin()`이 로그인 토큰도 함께 확인하도록 변경
관련 항목: **#2** (관리자 판별 클라이언트 위조 가능).

**변경 내용 (app.js:641):** `checkIsAdmin()`이 기존 학년/반/번호/이름 비교에 더해 `currentUser.adminToken`이 존재하는지도 확인하도록 수정. 1번 수정으로 이 토큰은 실제 관리자 비밀번호로 로그인해야만 서버가 발급해주므로, devtools에서 프로필 값만 대충 조작해서는 더 이상 관리자 UI가 뜨지 않음.

**한계(솔직히 밝힘):** 이건 완전한 보안 장치는 아닙니다 — 클라이언트 코드는 원리적으로 사용자가 다 들여다보고 조작할 수 있어서, `adminToken` 필드에 아무 문자열이나 채워 넣으면 이 클라이언트 쪽 체크는 여전히 통과됩니다. 진짜 방어선은 1번 수정으로 이미 만들어진 **서버 측 토큰 검증**입니다 — 실제 관리자 액션(삭제/조회/설정변경)은 그 가짜 토큰으로는 서버가 다 거부합니다. 이번 수정은 UI가 잘못된 상태를 보여주지 않도록 하는 보정 성격이 큽니다.

**남은 연결 문제:** `getContestStatus()`(app.js:649)가 여전히 `checkIsAdmin()`이 참이면 모든 공모전을 "active"로 반환해서, 위 한계와 마찬가지로 정교하게 값을 조작하면 마감된 공모전의 제출 폼을 열 수는 있습니다. 다만 실제로 제출이 접수되려면 `submitContest`가 서버에서 검증을 해야 하는데 그게 아직 안 되어 있어서(**#4**), 이 부분의 완전한 해결은 #4 수정과 묶여 있습니다.

**검증 상태:** `node --check` 문법 확인 완료. 클라이언트 전용 변경이라 별도 배포(Apps Script) 불필요 — 프론트엔드가 배포되면 바로 적용됨.

**✅ 사용자 확인 (2026-08-14):** 라이브 사이트에서 관리자 로그인/대시보드 접근 문제 해결됨.

### 4번 항목 수정: `submitContest`에 회차/신원 검증 추가
관련 항목: **#4** (`submitContest` 검증 부재).

**논의된 범위 결정:** 키링/도서관 등 비-젭퀴즈 공모전도 서버에서 API 직접 제출을 막을지 사용자에게 확인 → **막기로 결정**. 나중에 이 공모전들을 열려면 코드를 고치거나 관리자 토큰으로 우회해야 함 (아직 잠금 해제용 관리자 UI는 없음).

**변경 내용 (app.js, `submitContest` 핸들러):**
1. **회차 검증**: `zepquiz_N` 제출은 Settings 시트의 `zepquiz_active_round`와 N이 일치할 때만 허용 (관리자 토큰이 있으면 예외).
2. **비-젭퀴즈 공모전 차단**: `zepquiz_`로 시작하지 않는 `contestId`는 관리자 토큰 없이는 전부 거부. **단, `pixelart_draft`(픽셀아트 에디터의 임시저장 내부 기능)는 실제 "공모전"이 아니라서 예외로 허용** — 처음에 이걸 놓쳐서 학생들의 임시저장 기능이 깨질 뻔했다가 구현 중 발견해서 고침.
3. **제출자 신원 검증**: `entry.studentUsername/studentGrade/studentClass/studentNumber/studentName`이 Users 시트의 실제 가입 정보와 정확히 일치해야만 접수. 하나라도 다르면 "제출자 정보가 가입된 학생 정보와 일치하지 않습니다" 오류.

**⚠️ 알려진 한계 (사용자와 논의 후 현재 범위로 확정):** 3번 신원 검증은 "존재하지 않는 가짜 신원으로 제출"만 막습니다. **진짜 존재하는 다른 학생을 사칭하는 건 막지 못합니다** — 이 시스템엔 사전에 정해진 학생 명부가 없고 `signUp`으로 누구나 자기가 원하는 학년/반/번호/이름을 등록하는 구조라서, "Users 시트에 그 조합이 있는지"만으로는 요청을 보낸 사람이 진짜 그 학생인지 증명할 수 없습니다. 실제로 다른(이미 가입된) 학생의 정보는 공개 갤러리에도 노출되어 있어서(예: 키링 갤러리 응답에 `studentName`/`studentGrade` 등이 그대로 포함됨), 그 값을 그대로 복사해서 보내면 비밀번호 없이도 이 검증을 통과합니다. 진짜 사칭까지 막으려면 관리자처럼 학생도 로그인 시 토큰을 발급받아 매 제출마다 검증하는 구조가 필요한데, 이건 학생용 로그인/제출/취소 등 전 경로에 손대는 별도의 큰 작업이라 **이번 범위에서는 제외하기로 결정함** (2026-08-14, 사용자 확인).

**검증 상태:** `node --check` 문법 확인 완료. 클라이언트 정상 제출 흐름(모든 실제 제출은 로그인된 `currentUser` 값을 그대로 사용)은 항상 Users 시트와 일치하므로 영향 없음.

**✅ 배포 및 검증 완료 (2026-08-14):** 새 버전 배포 후 가짜 학생 정보로 4가지 케이스 curl 테스트 — 안 열린 회차 차단, 비-젭퀴즈 공모전 차단, (열린 회차/`pixelart_draft`) + 가짜 신원은 각각 신원 검증 단계에서 차단, 전부 의도대로 작동 확인. 테스트는 전부 거부되도록 설계해서 실제 시트에 데이터가 쓰이지 않음.


### 5번 항목 수정: 젭퀴즈 1회차 클램프 버그
관련 항목: **#5**.

**변경 내용 (app.js:29):** `fetchContestLocks()`에서 서버가 알려준 활성 회차를 `parsedRound < 2 ? 3 : parsedRound`로 되돌리던 조건을 `parsedRound < 1 ? 3 : parsedRound`로 수정. 기존엔 관리자가 1회차를 활성으로 설정해도(`setAsActiveZepRound`는 정상 저장됨) 학생 화면이 새로고침되거나 `fetchContestLocks()`가 다시 호출될 때마다 3회차로 강제로 되돌아갔음.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요, `main`에 병합 후 프론트엔드만 배포되면 바로 적용됨.

**검증 상태:** `node --check` 문법 확인 완료.

### 6번 항목 수정: 젭퀴즈 회차 조회 레이스 컨디션
관련 항목: **#6**.

**변경 내용 (app.js, `fetchZepQuizDataAndRender`):** 관리자가 회차 드롭다운을 빠르게 여러 번 바꾸면(`handleZepRoundChange`가 매번 이 함수를 await 없이 호출) 오래된 요청의 응답이 최신 요청보다 늦게 도착해 화면을 이전 회차 데이터로 덮어쓸 수 있었음. 요청마다 증가하는 토큰(`zepQuizFetchRequestToken`)을 발급해서, 응답이 왔을 때 "이 요청이 여전히 가장 최근 요청인지" 확인하고 아니면 그냥 버리도록 수정.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. 레이스 컨디션이라 실제 브라우저에서 회차를 빠르게 연타 전환해보며 확인하는 게 가장 확실함 (제가 재현하기는 어려움).

### 7번 항목 수정: 공모전 상세 드로어 레이스 컨디션
관련 항목: **#7**. 6번과 같은 종류의 문제(오래된 비동기 응답이 최신 화면을 덮어씀)를 같은 패턴(요청 토큰)으로 해결.

**변경 내용 (app.js, `checkAndRenderSubmissionArea`):** 학생이 공모전 상세 드로어를 빠르게 열었다 닫고 다른 공모전을 열면, 먼저 연 공모전의 "제출 내역 조회" 응답이 나중에 도착해 지금 보고 있는 다른 공모전의 제출 상태(제출완료/미제출 폼)를 잘못 덮어쓸 수 있었음. `submissionAreaRequestToken`을 추가해서, 서버 응답을 받은 시점에 "내가 여전히 가장 최근 요청인지" 확인 후 아니면 화면 갱신을 건너뛰도록 수정.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. 레이스 컨디션이라 실제로는 드로어를 빠르게 여러 번 전환해보며 확인하는 게 가장 확실함.

### 8번 항목 수정: AI 캘리그라피 생성 레이스 컨디션
관련 항목: **#8**. 6·7번과 같은 종류의 문제(오래된 비동기 작업이 최신 상태를 덮어씀).

**변경 내용 (app.js, `generateAICalligraphyCard`):** 캘리그라피 엽서 생성은 웹폰트 로딩 대기 때문에 최대 5초 정도 걸릴 수 있는데, 그 사이 학생이 도서관 공모전 드로어를 닫고 다른 공모전(예: 픽셀아트)으로 이동해 실제 파일을 업로드하면, 뒤늦게 완성된 캘리그라피 이미지가 공유 변수 `uploadBase64Data`를 덮어써서 엉뚱한(캘리그라피) 이미지가 제출될 수 있었음. 생성 시작 시점의 `activeContest`를 기억해뒀다가, 이미지 생성이 끝나 결과를 반영하기 직전에 여전히 같은 공모전을 보고 있는지 확인하고, 아니면 결과 반영을 건너뛰도록 수정 (성공 경로와 이미지 로드 실패 시의 폴백 경로 둘 다).

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. 레이스 컨디션이라 실제로는 캘리그라피 생성을 누른 직후 바로 다른 공모전으로 이동해 업로드해보며 확인하는 게 가장 확실함.

### 9·10번 항목 수정: 관리자 UI의 미구현 버튼 두 개
관련 항목: **#9, #10**.

**범위 판단:** 두 버튼 모두 "아직 실제로 만들어지지 않은 기능"이라는 게 공통점입니다. 이 코드베이스에는 이미 "준비 중" 안내로 처리하는 기존 관례가 있어서(표 뷰의 "표 뷰는 준비 중입니다" 정적 문구, 미등록 젭퀴즈 4~6회차 링크의 "아직 등록되지 않았습니다 (준비 중)"), 새 기능을 새로 설계해서 만들기보다 그 관례를 따라 **일관되게 "준비 중" 처리**했습니다. 진짜 기능 구현은 별도 작업으로 남겨둠.

**9번 (app.js, `window.openAdminEvalModal`):** 정의 자체가 없어서 클릭 시 콘솔 에러만 나던 함수를 새로 추가 — 클릭하면 "상세 평가 기능은 아직 준비 중입니다" 토스트만 뜨도록 함. 최소한 사용자에게 뭔가 반응은 보여줌.

**10번 (app.js, `setAdminViewMode`):** "표 뷰"로 전환할 때 호출하던 `renderAdminSubmissionsTable()`을 제거. 이 함수는 실제로는 숨겨진 갤러리 컨테이너에 렌더링해서 아무 의미가 없었고, 어차피 index.html에 있는 정적 "표 뷰는 준비 중입니다" 문구만 보였음 — 이제 그 안내만 그대로 보여주고 쓸데없는 렌더링 작업은 생략함.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료.

### 앞으로의 작업 규칙
- 코드를 수정할 때마다 이 문서 하단에 날짜/변경 내용/관련 항목 번호를 추가 기록한다.

---

## 2026-08-15

### 9월 온라인 도서관 — AI 캘리그라피 서체 버그 수정
사용자 요청: "AI 엽서에서 폰트가 제대로 적용되지 않아." 원본 리뷰 항목과는 무관한 별도 이슈.

**진단:** `sub-calli-font` 드롭다운의 7개 서체 중 5개(독도체/나눔붓체/나눔펜체/감자꽃체/연성체)는 구글 폰트로 정상 연결되어 있어 실제 라이브 사이트에서 캔버스에 정상 렌더링되는 것을 직접 확인함. 반면 **"사용자 에셋 서체 1/2"(CustomFont1/CustomFont2)는 `@font-face` 선언이 코드베이스 어디에도 없고 `asset/fonts/` 폴더 자체가 존재하지 않아서, 선택해도 항상 시스템 기본 폰트로 조용히 대체**되고 있었음 (라이브 사이트에서 직접 캔버스 렌더링 테스트로 확인).

**해결 방향 논의:** 실제 폰트 파일을 추가해서 살릴지, 아예 드롭다운에서 제거할지 사용자에게 확인 → **제거하기로 결정**.

**변경 내용 (app.js, 캘리그라피 서체 선택 `<select>`):** "사용자 에셋 서체 1/2" `<option>` 2개와 그 아래 안내 문구("개인 폰트는 asset/fonts/custom1.ttf 경로로...")를 제거. 이제 실제로 작동하는 5개 구글 폰트만 노출됨.

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. `CustomFont` 문자열이 코드베이스 어디에도 남아있지 않음을 grep으로 재확인.

### 9월 온라인 도서관 — 캘리그라피 서체 "일부 글자만 다르게 찍히는" 버그 수정 (2차)
사용자가 위 수정 이후에도 문제 스크린샷을 보내옴: 같은 문구("안녕하세요, 반갑습니다") 안에서 일부 글자는 선택한 서체로, 일부는 다른(기본) 서체로 섞여서 렌더링됨.

**원인 재조사:** 라이브 사이트에서 직접 재현 테스트. 기존 코드는 폰트 로딩 완료를 "캔버스에 시험 삼아 글자를 그려서 폭이 시스템 기본 폰트와 달라지는 순간"으로 판단했는데(`document.fonts.load()`는 await 없이 fire-and-forget으로만 호출), 브라우저가 폰트의 가로 폭 정보는 먼저 확정하고 실제 글자 모양(글리프)은 뒤이어 준비하는 경우가 있어 그 타이밍에 캔버스에 그리면 일부 글자만 옛날 폰트로 찍히는 것으로 추정됨. 실제로 `document.fonts.load()`를 제대로 `await`하는 방식으로 같은 문구를 5개 서체 전부 그려보니 전부 깨짐 없이 일관되게 렌더링되는 것을 확인해서 원인을 특정함.

**변경 내용 (app.js, `generateAICalligraphyCard`):** 기존의 "캔버스 텍스트 폭 비교" 방식 폰트 로딩 검증 로직을 전부 제거하고, 브라우저가 직접 보장하는 `document.fonts.load()` + `document.fonts.ready`를 제대로 `await`하는 방식으로 교체 (5초 타임아웃은 유지해서 네트워크 문제로 무한 대기하지 않도록 함).

**순수 프론트엔드 수정** — Apps Script 재배포 불필요.

**검증 상태:** `node --check` 문법 확인 완료. 새 방식(`await document.fonts.load()` + `document.fonts.ready`)으로 5개 서체 전부 라이브 사이트에서 직접 그려서 글자 섞임 없이 일관되게 렌더링되는 것을 스크린샷으로 확인.
